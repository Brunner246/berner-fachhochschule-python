{"config": {"lang": ["en"], "separator": "[\\s\\-]+", "pipeline": ["stopWordFilter"], "fields": {"title": {"boost": 1000.0}, "text": {"boost": 1.0}, "tags": {"boost": 1000000.0}}}, "docs": [{"location": "", "title": "Python &amp; cadwork 3d API", "text": "<p>Welcome to the lecture material for Python programming and the cadwork 3d Python API at Berner Fachhochschule.</p> <p></p>"}, {"location": "#course-overview", "title": "Course Overview", "text": "<p>This course is structured into two main parts:</p> <ol> <li>Python Basics \u2014 Core Python concepts including data types, control flow, functions, and object-oriented programming.</li> <li>cadwork 3d API \u2014 Applying Python within cadwork 3d to automate timber construction workflows.</li> </ol>"}, {"location": "cadwork-api/", "title": "cadwork 3d Python API", "text": "<p>The cadwork 3d Python API allows you to automate and extend cadwork 3d using Python scripts. This section covers how to get started and work with elements programmatically.</p>"}, {"location": "cadwork-api/#topics", "title": "Topics", "text": "<ul> <li>Getting Started \u2014 Setting up Python in cadwork 3d</li> <li>Debugging \u2014 Tips for troubleshooting your scripts</li> <li>Geometry \u2014 Points, vectors, and the unit circle</li> <li>Elements \u2014 Creating and manipulating elements</li> <li>Attributes \u2014 Reading and writing element attributes</li> <li>AI &amp; MCP \u2014 Model Context Protocol for the cadwork API</li> </ul>"}, {"location": "cadwork-api/#exercises", "title": "Exercises", "text": "<p>Hands-on exercises using a timber framed slab model, progressing from reading to creating:</p> <ol> <li>Reading Data \u2014 Query elements, filter by name, group and export</li> <li>Geometry \u2014 Dimensions, volumes, positions, and spacing</li> <li>Create &amp; Modify \u2014 Build and modify elements programmatically</li> <li>Capstone Project \u2014 Build a model generation script from scratch</li> <li>Cheatsheet \u2014 Quick reference for common API calls</li> </ol> <p>Info</p> <p>The cadwork Python API is available in cadwork 3d version 30 and later.</p>"}, {"location": "cadwork-api/attributes/", "title": "Attributes", "text": "<p>Attributes store metadata about elements such as name, group, material, and user-defined properties.</p>"}, {"location": "cadwork-api/attributes/#reading-attributes", "title": "Reading Attributes", "text": "<pre><code>import attribute_controller as ac\n\nelement_id = 1001\n\nname = ac.get_name(element_id)\ngroup = ac.get_group(element_id)\nsubgroup = ac.get_subgroup(element_id)\nmaterial = ac.get_material(element_id)\n\nprint(f\"{name} | {group} | {material}\")\n</code></pre>"}, {"location": "cadwork-api/attributes/#writing-attributes", "title": "Writing Attributes", "text": "<pre><code>import attribute_controller as ac\n\nelement_ids = [1001, 1002]\n\nfor eid in element_ids:\n    ac.set_name(eid, \"Rafter\")\n    ac.set_group(eid, \"Roof\")\n    ac.set_subgroup(eid, \"Primary Structure\")\n</code></pre>"}, {"location": "cadwork-api/attributes/#user-attributes", "title": "User Attributes", "text": "<p>cadwork supports user-defined attributes (user attributes 1\u201399):</p> <pre><code>import attribute_controller as ac\n\nelement_id = 1001\n\n# Set a user attribute\nac.set_user_attribute(element_id, 1, \"Custom Value\")\n\n# Read it back\nvalue = ac.get_user_attribute(element_id, 1)\nprint(value)  # Custom Value\n</code></pre>"}, {"location": "cadwork-api/attributes/#listing-all-attributes", "title": "Listing All Attributes", "text": "<pre><code>import element_controller as ec\nimport attribute_controller as ac\n\nfor eid in ec.get_active_identifiable_element_ids():\n    print(\n        f\"ID: {eid}, \"\n        f\"Name: {ac.get_name(eid)}, \"\n        f\"Material: {ac.get_material(eid)}\"\n    )\n</code></pre> <p>Warning</p> <p>Setting attributes on a large number of elements in a loop can be slow. Consider batching operations where possible.</p>"}, {"location": "cadwork-api/capstone/", "title": "Capstone \u2014 Build a Timber Frame from Scratch", "text": "<p>This is the integrative end-of-workshop exercise. It combines everything you have learned: defining geometry from parameters, creating new elements, modifying attributes, coloring for visual feedback, and exporting a Bill of Materials.</p> <p>You will start with an empty cadwork 3d file and end with a complete timber-framed floor \u2014 rim joists, joists, blocking, named, numbered, colored, plus a BoM CSV next to your model file.</p> <p>Setup</p> <ol> <li>Open cadwork 3d and create a new, empty 3d file (<code>File \u2192 New</code>).</li> <li>Save it once so it has a path on disk (the BoM export needs that path).</li> <li>No selection, no model download, no template \u2014 your script will create every element from scratch.</li> </ol>"}, {"location": "cadwork-api/capstone/#the-brief", "title": "The brief", "text": "<p>Write one script <code>frame_generator.py</code> that does all of the following, in order:</p> <ol> <li>Define the frame dimensions and cross-sections as constants at the top of the script.</li> <li>Generate joists along the X axis, spaced at 625 mm center-to-center.</li> <li>Generate two rim joists along the Y axis that cap the ends of the joist array.</li> <li>Generate blocking at the midpoint of each joist bay.</li> <li>Name every element (<code>Joist</code>, <code>Rim Joist</code>, <code>Blocking</code>) and assign group <code>\"Slab\"</code>, subgroup <code>\"Structure\"</code>.</li> <li>Number joists sequentially: <code>J-001</code>, <code>J-002</code>, \u2026 stored in user attribute 1.</li> <li>Color joists red, rim joists green, blocking blue.</li> <li>Export a CSV bill of materials next to the model file: columns <code>Name</code>, <code>Count</code>, <code>Total Length (m)</code>, <code>Total Volume (m\u00b3)</code>.</li> </ol> <pre><code>flowchart LR\n    A[\"Empty 3d file\"] --&gt; B[\"Define constants\"]\n    B --&gt; C[\"Generate joists\"]\n    C --&gt; D[\"Generate rim joists\"]\n    D --&gt; E[\"Generate blocking\"]\n    E --&gt; F[\"Name + group + number\"]\n    F --&gt; G[\"Color\"]\n    G --&gt; H[\"Export BoM CSV\"]</code></pre> <pre><code>flowchart TB\n    subgraph \"Top view of the finished frame\"\n        direction LR\n        R1[\"Rim @ x=0\"] ~~~ J1[\"Joist y=0\"] ~~~ J2[\"Joist y=625\"] ~~~ JN[\"\u2026 10 joists\"] ~~~ R2[\"Rim @ x=5000\"]\n    end</code></pre>"}, {"location": "cadwork-api/capstone/#constants-to-use", "title": "Constants to use", "text": "<pre><code>JOIST_LENGTH   = 5000.0   # mm, span of each joist along X\nJOIST_COUNT    = 10       # number of joists\nJOIST_SPACING  = 625.0    # mm, center-to-center along Y\nJOIST_WIDTH    = 60.0     # mm\nJOIST_HEIGHT   = 240.0    # mm\n\nCOLOR_JOIST    = 3        # red\nCOLOR_RIM      = 4        # green\nCOLOR_BLOCKING = 5        # blue\n</code></pre> <p>Everything else is derived from these \u2014 no magic numbers in the body of the script.</p>"}, {"location": "cadwork-api/capstone/#build-it-incrementally", "title": "Build it incrementally", "text": "<p>Don't try to write the whole script at once. Build it in stages, running each stage to verify before moving on. The visualization step in each stage is your safety net.</p> Stage 1 \u2014 Generate joists <p>Joists run along X from <code>(0, y, 0)</code> to <code>(LENGTH, y, 0)</code>. The Y position is <code>i * SPACING</code> for joist <code>i</code>.</p> <pre><code>import cadwork as cw\nimport element_controller as ec\nimport attribute_controller as ac\n\nJOIST_LENGTH  = 5000.0\nJOIST_COUNT   = 10\nJOIST_SPACING = 625.0\nJOIST_WIDTH   = 60.0\nJOIST_HEIGHT  = 240.0\n\nx_direction = cw.point_3d(1, 0, 0)\njoist_ids = []\n\nfor i in range(JOIST_COUNT):\n    p1 = cw.point_3d(0, i * JOIST_SPACING, 0)\n    new_id = ec.create_rectangular_beam_vectors(\n        JOIST_WIDTH, JOIST_HEIGHT, JOIST_LENGTH, p1, x_direction\n    )\n    ac.set_name([new_id], \"Joist\")\n    joist_ids.append(new_id)\n\nprint(f\"Generated {len(joist_ids)} joists\")\n</code></pre> <p>Run this. You should see 10 parallel beams in the viewport.</p> Stage 2 \u2014 Generate the two rim joists <p>Rim joists run along Y at <code>x = 0</code> and <code>x = JOIST_LENGTH</code>. They extend from <code>y = -WIDTH/2</code> to <code>y = (COUNT-1) * SPACING + WIDTH/2</code> so they cap the outer faces of the first and last joist.</p> <pre><code>y_direction = cw.point_3d(0, 1, 0)\nrim_start_y = -JOIST_WIDTH / 2\nrim_length  = (JOIST_COUNT - 1) * JOIST_SPACING + JOIST_WIDTH\n\nrim_ids = []\nfor x in (0.0, JOIST_LENGTH):\n    p1 = cw.point_3d(x, rim_start_y, 0)\n    new_id = ec.create_rectangular_beam_vectors(\n        JOIST_WIDTH, JOIST_HEIGHT, rim_length, p1, y_direction\n    )\n    ac.set_name([new_id], \"Rim Joist\")\n    rim_ids.append(new_id)\n\nprint(f\"Generated {len(rim_ids)} rim joists\")\n</code></pre> Stage 3 \u2014 Blocking at mid-span <p>Blocking runs along Y between adjacent joists at <code>x = JOIST_LENGTH / 2</code>. Each block fills the clear space between joist faces: from <code>i * SPACING + WIDTH/2</code> to <code>(i+1) * SPACING - WIDTH/2</code>.</p> <pre><code>mid_x = JOIST_LENGTH / 2\nblock_length = JOIST_SPACING - JOIST_WIDTH\nblocking_ids = []\n\nfor i in range(JOIST_COUNT - 1):\n    y_start = i * JOIST_SPACING + JOIST_WIDTH / 2\n    p1 = cw.point_3d(mid_x, y_start, 0)\n    new_id = ec.create_rectangular_beam_vectors(\n        JOIST_WIDTH, JOIST_HEIGHT, block_length, p1, y_direction\n    )\n    ac.set_name([new_id], \"Blocking\")\n    blocking_ids.append(new_id)\n\nprint(f\"Generated {len(blocking_ids)} blocking elements\")\n</code></pre> Stage 4 \u2014 Group, subgroup, sequential numbering <pre><code>all_new = joist_ids + rim_ids + blocking_ids\nfor eid in all_new:\n    ac.set_group([eid], \"Slab\")\n    ac.set_subgroup([eid], \"Structure\")\n\nfor i, eid in enumerate(joist_ids, start=1):\n    ac.set_user_attribute([eid], 1, f\"J-{i:03d}\")\n</code></pre> Stage 5 \u2014 Color <pre><code>import visualization_controller as vc\n\nCOLOR_JOIST    = 3\nCOLOR_RIM      = 4\nCOLOR_BLOCKING = 5\n\nvc.set_color(joist_ids,    COLOR_JOIST)\nvc.set_color(rim_ids,      COLOR_RIM)\nvc.set_color(blocking_ids, COLOR_BLOCKING)\n</code></pre> <p>Run this and look at the viewport \u2014 the frame should be lit up in three colors. If something is the wrong color, you have a bug in an earlier stage's ID list.</p> Stage 6 \u2014 Bill of Materials CSV <pre><code>import csv, os\nimport geometry_controller as gc\nimport utility_controller as uc\n\ngroups = {\n    \"Joist\":     joist_ids,\n    \"Rim Joist\": rim_ids,\n    \"Blocking\":  blocking_ids,\n}\n\nrows = []\nfor name, ids in groups.items():\n    total_length_mm = sum(gc.get_length(e) for e in ids)\n    total_volume_mm3 = sum(\n        gc.get_length(e) * gc.get_width(e) * gc.get_height(e) for e in ids\n    )\n    rows.append({\n        \"Name\": name,\n        \"Count\": len(ids),\n        \"Total Length (m)\": round(total_length_mm / 1000, 2),\n        \"Total Volume (m\u00b3)\": round(total_volume_mm3 / 1_000_000_000, 4),\n    })\n\noutput_path = os.path.join(uc.get_3d_file_path(), \"bom.csv\")\nwith open(output_path, \"w\", newline=\"\") as f:\n    writer = csv.DictWriter(f, fieldnames=rows[0].keys())\n    writer.writeheader()\n    writer.writerows(rows)\n\nprint(f\"BoM exported to {output_path}\")\n</code></pre>"}, {"location": "cadwork-api/capstone/#full-reference-solution", "title": "Full reference solution", "text": "<p>The assembled full script is on a separate, password-protected page:</p> <p>:lock: Open the full solution</p> <p>Ask your lecturer for the password during the workshop. Try the staged hints above first \u2014 you'll learn more from getting stuck and unstuck than from copying the finished script.</p>"}, {"location": "cadwork-api/capstone/#stretch-goals", "title": "Stretch goals", "text": "<p>If you finish early, try one of these extensions:</p> <ol> <li>Add sheathing: a single rectangular panel on top of the frame covering the whole footprint.</li> <li>Make spacing configurable: read <code>JOIST_SPACING</code> from user input via <code>utility_controller.get_user_double()</code>.</li> <li>Derive <code>JOIST_COUNT</code> from a target frame width instead of hard-coding it (e.g., fit as many joists as needed to span 6000 mm at the given spacing).</li> <li>Add an opening: skip joists in a <code>1500 \u00d7 1500 mm</code> region (e.g., a stair opening) \u2014 print which joists were skipped.</li> </ol> <p>You made it</p> <p>If your viewport now shows a colored, named, fully numbered floor frame and a <code>bom.csv</code> sits next to your model file, congratulations \u2014 you have written your first real cadwork generator. Save this script: it is your starting point for everything you will write after the workshop.</p>"}, {"location": "cadwork-api/cheatsheet/", "title": "Cheat Sheet", "text": "<p>One-page reference of the most-used cadwork 3d Python API calls. Print it, keep it next to your keyboard.</p>"}, {"location": "cadwork-api/cheatsheet/#imports", "title": "Imports", "text": "<pre><code>import cadwork as cw\nimport element_controller as ec\nimport attribute_controller as ac\nimport geometry_controller as gc\nimport visualization_controller as vc\nimport utility_controller as uc\n</code></pre>"}, {"location": "cadwork-api/cheatsheet/#element_controller-query-create-delete", "title": "<code>element_controller</code> \u2014 query, create, delete", "text": "Call Purpose <code>ec.get_all_identifiable_element_ids()</code> All element IDs in the model <code>ec.get_active_identifiable_element_ids()</code> Currently selected IDs <code>ec.get_visible_identifiable_element_ids()</code> Currently visible IDs <code>ec.create_rectangular_beam_vectors(w, h, l, p1, xl_dir, zl_dir)</code> New beam from origin + direction <code>ec.copy_elements(ids)</code> Duplicate, returns new IDs <code>ec.move_element(ids, vector)</code> Translate by a <code>point_3d</code> offset <code>ec.delete_elements(ids)</code> Remove from model"}, {"location": "cadwork-api/cheatsheet/#attribute_controller-read-write-metadata", "title": "<code>attribute_controller</code> \u2014 read &amp; write metadata", "text": "Call Purpose <code>ac.get_name(eid)</code> / <code>ac.set_name([eid], \"\u2026\")</code> Element name <code>ac.get_group(eid)</code> / <code>ac.set_group([eid], \"\u2026\")</code> Group <code>ac.get_subgroup(eid)</code> / <code>ac.set_subgroup([eid], \"\u2026\")</code> Subgroup <code>ac.get_element_material_name(eid)</code> Material as a string <code>ac.set_material([eid], \"GL24h\")</code> Assign material by name <code>ac.get_user_attribute(eid, n)</code> / <code>ac.set_user_attribute([eid], n, \"\u2026\")</code> User attribute slot 1\u201399 <code>ac.get_element_type(eid)</code> Returns a type object with <code>.is_rectangular_beam()</code> etc. <code>ac.set_assembly_number([eid], \"\u2026\")</code> Assembly number <p>Setters take a list of IDs (even if only one) and a single value.</p>"}, {"location": "cadwork-api/cheatsheet/#geometry_controller-sizes-and-positions", "title": "<code>geometry_controller</code> \u2014 sizes and positions", "text": "Call Purpose <code>gc.get_length(eid)</code> mm along the element's local X (xl) <code>gc.get_width(eid)</code> mm along yl <code>gc.get_height(eid)</code> mm along zl <code>gc.get_p1(eid)</code> / <code>gc.get_p2(eid)</code> Start / end as <code>point_3d</code> <code>gc.get_xl(eid)</code> / <code>gc.get_yl(eid)</code> / <code>gc.get_zl(eid)</code> Local basis vectors <code>gc.set_width_real([eid], mm)</code> Change width (cross-section) <code>gc.set_height_real([eid], mm)</code> Change height"}, {"location": "cadwork-api/cheatsheet/#visualization_controller-see-your-results", "title": "<code>visualization_controller</code> \u2014 see your results", "text": "Call Purpose <code>vc.set_color(ids, color_id)</code> Paint elements (color id, integer) <code>vc.set_active_element_ids(ids)</code> Select in viewport <code>vc.hide_elements(ids)</code> / <code>vc.show_elements(ids)</code> Visibility <code>vc.show_all_elements()</code> Reset hidden state <code>vc.show_view_standard_axo()</code> Reset to isometric <code>vc.show_view_positive_z()</code> Top view"}, {"location": "cadwork-api/cheatsheet/#utility_controller-files-user-io", "title": "<code>utility_controller</code> \u2014 files &amp; user I/O", "text": "Call Purpose <code>uc.get_3d_file_path()</code> Folder of the current <code>.3d</code> file \u2014 useful for exports <code>uc.get_3d_file_name()</code> Current model filename <code>uc.get_user_string(prompt)</code> Ask the user for a string <code>uc.get_user_double(prompt)</code> Ask for a number <code>uc.get_user_int(prompt)</code> Ask for an integer <code>uc.print_to_console(msg)</code> Print to cadwork's script console"}, {"location": "cadwork-api/cheatsheet/#cadwork-core-types-math", "title": "<code>cadwork</code> core \u2014 types &amp; math", "text": "Call Purpose <code>cw.point_3d(x, y, z)</code> Create a 3D point/vector <code>p1 + p2</code>, <code>p1 - p2</code> Vector arithmetic <code>p * 2.5</code> Scale <code>p1.distance(p2)</code> Length between <code>v.normalized()</code> Unit vector <code>v.cross(other)</code> Cross product (perpendicular) <code>v.dot(other)</code> Dot product (angle / projection) <code>p.x</code>, <code>p.y</code>, <code>p.z</code> Component access"}, {"location": "cadwork-api/cheatsheet/#common-python-helpers", "title": "Common Python helpers", "text": "<pre><code># Filter elements by name\njoists = [eid for eid in all_ids if ac.get_name(eid) == \"Joist\"]\n\n# Group by name and count\nfrom collections import Counter\ncounts = Counter(ac.get_name(eid) for eid in all_ids)\n\n# Unique values\nmaterials = {ac.get_element_material_name(eid) for eid in all_ids}\n\n# CSV export\nimport csv, os\npath = os.path.join(uc.get_3d_file_path(), \"report.csv\")\nwith open(path, \"w\", newline=\"\") as f:\n    writer = csv.writer(f)\n    writer.writerow([\"ID\", \"Name\"])\n    for eid in all_ids:\n        writer.writerow([eid, ac.get_name(eid)])\n\n# Math\nimport math\nmath.floor(rim_length / spacing) + 1\n</code></pre>"}, {"location": "cadwork-api/cheatsheet/#workflow-reminders", "title": "Workflow reminders", "text": "<ul> <li>Element IDs change between sessions \u2014 never hard-code them.</li> <li>All lengths are in mm.</li> <li>Setters take lists of IDs: <code>ac.set_name([eid], \"Joist\")</code>.</li> <li><code>vc.set_color(ids, 3)</code> is your visual unit-test.</li> <li>Read tracebacks from the bottom up \u2014 last line tells you what broke.</li> <li>Save the model before running anything that creates or deletes elements.</li> </ul>"}, {"location": "cadwork-api/debugging/", "title": "Debugging &amp; Common Pitfalls", "text": "<p>Every script eventually breaks. This page is your rescue reference when something goes wrong \u2014 read a traceback, recognize the error, apply the fix, verify visually.</p>"}, {"location": "cadwork-api/debugging/#read-the-traceback-bottom-up", "title": "Read the traceback bottom-up", "text": "<p>When Python prints an error, the last line tells you what broke. The lines above it tell you where it broke.</p> <pre><code>Traceback (most recent call last):\n  File \"frame.py\", line 12, in &lt;module&gt;\n    p1 = gc.get_p1(eid)\n  File \"...\", line 47, in get_p1\n    raise RuntimeError(\"Element not found: 1042\")\nRuntimeError: Element not found: 1042\n</code></pre> <p>Read it like this:</p> <ol> <li>Last line \u2192 <code>RuntimeError: Element not found: 1042</code> \u2014 what happened.</li> <li>Second-to-last block \u2192 the API call that raised the error.</li> <li>First block in your file \u2192 the line in your code that triggered it (<code>line 12</code>).</li> </ol> <p>Fix priority: find the line in your file that's named in the traceback. The fix is almost always there.</p> <p></p>"}, {"location": "cadwork-api/debugging/#common-errors-and-what-they-mean", "title": "Common errors and what they mean", "text": ""}, {"location": "cadwork-api/debugging/#attributeerror-module-element_controller-has-no-attribute-get_all_identifiable_elements", "title": "<code>AttributeError: module 'element_controller' has no attribute 'get_all_identifiable_elements'</code>", "text": "<p>You mistyped a function name. Compare letter-by-letter against the docs or the cheat sheet. Common slips:</p> <ul> <li><code>get_all_identifiable_element_ids</code> (correct) vs. <code>get_all_identifiable_elements</code> (missing <code>_ids</code>)</li> <li><code>get_element_material_name</code> (correct) vs. <code>get_material_name</code></li> </ul> <p><code>dir(element_controller)</code> in a Python prompt lists everything available.</p>"}, {"location": "cadwork-api/debugging/#typeerror-set_color-takes-2-positional-arguments-but-3-were-given", "title": "<code>TypeError: set_color() takes 2 positional arguments but 3 were given</code>", "text": "<p>You passed arguments in the wrong order or wrong count. <code>set_color(ids, color)</code> takes two: a list of IDs and a color id. Forgetting that <code>ids</code> must be a list (<code>[eid]</code> not <code>eid</code>) is the #1 cause of this error in cadwork scripts.</p>"}, {"location": "cadwork-api/debugging/#indexerror-list-index-out-of-range", "title": "<code>IndexError: list index out of range</code>", "text": "<p>You assumed a list had elements when it didn't.</p> <pre><code>joists = [e for e in all_ids if ac.get_name(e) == \"Joist\"]\nfirst = joists[0]   # IndexError if the model has no joists\n</code></pre> <p>Fix: check first.</p> <pre><code>if not joists:\n    print(\"No joists found\")\nelse:\n    first = joists[0]\n</code></pre>"}, {"location": "cadwork-api/debugging/#keyerror-material", "title": "<code>KeyError: 'material'</code>", "text": "<p>You accessed a dict key that doesn't exist. Use <code>.get()</code> with a default:</p> <pre><code>beam.get(\"material\", \"unknown\")\n</code></pre>"}, {"location": "cadwork-api/debugging/#script-runs-but-does-nothing", "title": "Script runs but does nothing", "text": "<p>No traceback, no output, no model change. Walk through:</p> <ol> <li>Did you <code>print()</code> anything? If yes, where did the output go? (cadwork shows it in the console.)</li> <li>Did you call a <code>set_*</code> function on an empty list? <code>set_name([], \"Joist\")</code> is a no-op.</li> <li>Are you using the right module \u2014 did you mean <code>ec</code> (element) but type <code>ac</code> (attribute)?</li> <li>Is anything selected? <code>get_active_identifiable_element_ids()</code> returns <code>[]</code> if no element is selected.</li> </ol>"}, {"location": "cadwork-api/debugging/#sanity-check-checklist-before-asking-for-help", "title": "Sanity-check checklist before asking for help", "text": "<p>Walk through these before raising your hand:</p> <ul> <li>[ ] Is <code>timber-framed-elements.3d</code> open in cadwork 3d?</li> <li>[ ] Is the right element selected (if your script uses <code>get_active_\u2026</code>)?</li> <li>[ ] Is the script in the right <code>userprofil_xxxx\\3d\\API.x64\\my_script\\</code> folder, with matching filename?</li> <li>[ ] Did you <code>import</code> every module you use (<code>ec</code>, <code>ac</code>, <code>gc</code>, <code>vc</code>, <code>uc</code>)?</li> <li>[ ] Did you read the last line of the traceback?</li> <li>[ ] Did you compare the function name against the cheat sheet?</li> </ul>"}, {"location": "cadwork-api/debugging/#workshop-survival-tips", "title": "Workshop survival tips", "text": "<p>Save before you run</p> <p>Every modification script can corrupt the model. Save the file before running anything that creates or deletes elements. <code>Ctrl+Z</code> works for most cadwork operations but is not always reliable for batch API changes.</p> <p>Print early, print often</p> <p><code>print(f\"After filter: {len(joists)} joists\")</code> after every major step. When something is wrong, the last successful print tells you exactly how far you got.</p> <p>ID lifetime</p> <p>Element IDs are valid only within the current cadwork session. Never write code that assumes ID <code>1042</code> is the same element tomorrow. Always look up elements by GUID, not by hard-coded ID.</p> <p>Units</p> <p>cadwork's Python API uses millimeters for all lengths and degrees for some angles (depending on the function). When in doubt, print a value and check the magnitude \u2014 a beam that prints <code>5.0</code> is probably 5 mm, not 5 m.</p>"}, {"location": "cadwork-api/elements/", "title": "Elements", "text": "<p>Elements are the core building blocks in cadwork 3d. The Python API lets you create, query, and manipulate them.</p>"}, {"location": "cadwork-api/elements/#querying-elements", "title": "Querying Elements", "text": "<pre><code>import element_controller as ec\n\n# Get all element IDs\nall_ids = ec.get_all_identifiable_element_ids()\n\n# Get active (selected) elements\nactive_ids = ec.get_active_identifiable_element_ids()\n\nprint(f\"Total: {len(all_ids)}, Selected: {len(active_ids)}\")\n</code></pre>"}, {"location": "cadwork-api/elements/#creating-elements", "title": "Creating Elements", "text": "<pre><code>import cadwork as cw\nimport element_controller as ec\n\n# Define a beam by start point, end point, and cross-section\np1 = cw.point_3d(0, 0, 0)\np2 = cw.point_3d(5000, 0, 0)\n\nlocal_x_dir = (p2 - p1).normalized()  # Direction from p1 to p2\nlocal_z_dir = cw.point_3d(0, 0, 1)          # Up direction\n\nwidth = 120.0\nheight = 240.0\nlength = 5000.0\n\nelement_id = ec.create_rectangular_beam_vectors(width, height, length, p1, local_x_dir, local_z_dir)\n</code></pre>"}, {"location": "cadwork-api/elements/#deleting-elements", "title": "Deleting Elements", "text": "<pre><code>import element_controller as ec\n\n# Delete specific elements\nelement_ids = [1001, 1002, 1003]\nec.delete_elements(element_ids)\n</code></pre>"}, {"location": "cadwork-api/elements/#filtering-elements", "title": "Filtering Elements", "text": "<pre><code>import element_controller as ec\nimport attribute_controller as ac\n\nall_ids = ec.get_all_identifiable_element_ids()\n\n# Filter beams by name\nbeams = [\n    eid for eid in all_ids\n    if ac.get_name(eid) == \"Beam\"\n]\n</code></pre> <p>Note</p> <p>Element IDs are integers that uniquely identify each element in the current cadwork session. They may change between sessions.</p>"}, {"location": "cadwork-api/exercises-create-modify/", "title": "Exercises \u2014 Creating and Modifying Elements", "text": "<p>These exercises build on the previous ones. You will now create new elements and modify existing ones in the timber framed slab model.</p> <p>Setup</p> <p>Download the timber framed slab model and open it in cadwork 3d before starting.</p> <p>Warning</p> <p>These exercises modify the model. Save a copy of the original file before starting so you can reset if needed.</p>"}, {"location": "cadwork-api/exercises-create-modify/#exercise-1-rename-elements", "title": "Exercise 1: Rename Elements", "text": "<p>Write a script that renames all elements currently named <code>\"Beam\"</code> to <code>\"Joist\"</code>.</p> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\n\nall_ids = ec.get_all_identifiable_element_ids()\nbeams = [eid for eid in all_ids if ac.get_name(eid) == \"Joist\"]\n\nfor eid in beams:\n    ac.set_name([eid], \"Beam\")\n\nprint(f\"Renamed {len(beams)} elements\")\n</code></pre>"}, {"location": "cadwork-api/exercises-create-modify/#exercise-2-set-group-and-subgroup", "title": "Exercise 2: Set Group and Subgroup", "text": "<p>Write a script that sets the group to <code>\"Slab\"</code> and the subgroup to <code>\"Structure\"</code> for all joists.</p> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\n\nall_ids = ec.get_all_identifiable_element_ids()\njoists = [eid for eid in all_ids if ac.get_name(eid) == \"Joist\"]\n\nfor eid in joists:\n    ac.set_group([eid], \"Slab\")\n    ac.set_subgroup([eid], \"Structure\")\n\nprint(f\"Updated {len(joists)} joists\")\n</code></pre>"}, {"location": "cadwork-api/exercises-create-modify/#exercise-3-assign-sequential-assembly-numbers", "title": "Exercise 3: Assign Sequential Assembly Numbers", "text": "<p>Write a script that assigns a sequential assembly number (via a user attribute) to every joist, sorted by their position along the Y-axis.</p> <p>Expected result: the joist closest to Y=0 gets <code>\"J-001\"</code>, the next one <code>\"J-002\"</code>, etc.</p> Hint <p>Sort joists by the Y-coordinate of their P1, then use <code>ac.set_user_attribute()</code> or <code>ac.set_assembly_number()</code> with a formatted string.</p> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\nimport geometry_controller as gc\n\nall_ids = ec.get_all_identifiable_element_ids()\njoists = [eid for eid in all_ids if ac.get_name(eid) == \"Beam\"]\n\n# Sort by X position\njoists_sorted = sorted(joists, key=lambda eid: gc.get_p1(eid).x)\n\nfor i, eid in enumerate(joists_sorted, start=1):\n    assembly_nr = f\"J-{i:03d}\"\n    ac.set_user_attribute([eid], 1, assembly_nr) # or ac.set_assembly_number([eid], assembly_nr)\n    print(f\"ID {eid} -&gt; {assembly_nr}\")\n\nprint(f\"Assigned assembly numbers to {len(joists_sorted)} joists\")\n</code></pre>"}, {"location": "cadwork-api/exercises-create-modify/#exercise-4-create-a-single-beam", "title": "Exercise 4: Create a Single Beam", "text": "<p>Write a script that creates a new beam with the following properties:</p> <ul> <li>Start point: <code>(0, 0, 0)</code></li> <li>End point: <code>(5000, 0, 0)</code></li> <li>Width: <code>120 mm</code></li> <li>Height: <code>240 mm</code></li> </ul> <p>After creating it, set the name to <code>\"New Joist\"</code>.</p> Solution <pre><code>import cadwork as cw\nimport element_controller as ec\nimport attribute_controller as ac\n\np1 = cw.point_3d(0, 0, 0)\np2 = cw.point_3d(5000, 0, 0)\n\nwidth = 120.0\nheight = 240.0\nlength = p2.distance(p1)\nlength_direction = (p2 - p1).normalized()\nlocal_z_direction = cw.point_3d(0, 0, 1) # Up direction\nnew_id = ec.create_rectangular_beam_vectors(width, height, length, p1, length_direction, local_z_direction)\nac.set_name([new_id], \"New Joist\")\n\nprint(f\"Created element {new_id}\")\n</code></pre>"}, {"location": "cadwork-api/exercises-create-modify/#exercise-5-create-a-row-of-joists", "title": "Exercise 5: Create a Row of Joists", "text": "<p>Write a script that creates a row of 10 joists spaced at 625 mm center-to-center. All joists should:</p> <ul> <li>Run along the X-axis from <code>x=0</code> to <code>x=5000</code></li> <li>Have a cross-section of <code>60 x 240 mm</code></li> <li>Be named <code>\"Joist\"</code></li> <li>Be assigned to group <code>\"Slab\"</code> and subgroup <code>\"Structure\"</code></li> </ul> <pre><code>flowchart TB\n    subgraph \"Top View (looking down)\"\n        direction LR\n        J1[\"J @ y=0\"] ~~~ J2[\"J @ y=625\"] ~~~ J3[\"J @ y=1250\"] ~~~ J4[\"...\"] ~~~ J10[\"J @ y=5625\"]\n    end</code></pre> Solution <pre><code>import cadwork as cw\nimport element_controller as ec\nimport attribute_controller as ac\n\nWIDTH = 60.0\nHEIGHT = 240.0\nLENGTH = 5000.0\nSPACING = 625.0\nCOUNT = 10\n\ncreated = []\nfor i in range(COUNT):\n    y = i * SPACING\n    p1 = cw.point_3d(0, y, 0)\n    p2 = cw.point_3d(LENGTH, y, 0)\n    length = p2.distance(p1)\n    length_direction = (p2 - p1).normalized()\n    local_z_direction = cw.point_3d(0, 0, 1) # Up direction\n\n    new_id = ec.create_rectangular_beam_vectors(WIDTH, HEIGHT, length, p1, length_direction, local_z_direction)\n    ac.set_name([new_id], \"Joist\")\n    ac.set_group([new_id], \"Slab\")\n    ac.set_subgroup([new_id], \"Structure\")\n    created.append(new_id)\n\nprint(f\"Created {len(created)} joists\")\n</code></pre>"}, {"location": "cadwork-api/exercises-create-modify/#exercise-6-add-rim-joists", "title": "Exercise 6: Add Rim Joists", "text": "<p>Extend the joist row from Exercise 5 by adding two rim joists that cap the ends of the row. The rim joists should:</p> <ul> <li>Run along the Y-axis, perpendicular to the joists</li> <li>Span from the first joist to the last joist (including half the joist width on each side)</li> <li>Have a cross-section of <code>60 x 240 mm</code></li> <li>Be named <code>\"Rim Joist\"</code></li> </ul> <pre><code>flowchart TB\n    subgraph \"Top View\"\n        R1[\"Rim Joist (x=0)\"]\n        J1[\"Joist\"] ~~~ J2[\"Joist\"] ~~~ J3[\"Joist\"]\n        R2[\"Rim Joist (x=5000)\"]\n        R1 --- J1\n        R1 --- J2\n        R1 --- J3\n        J1 --- R2\n        J2 --- R2\n        J3 --- R2\n    end</code></pre> Hint <p>The rim joists run along Y. Calculate the Y-span from <code>(-WIDTH/2)</code> to <code>((COUNT-1) * SPACING + WIDTH/2)</code> to cover the outer edges of the first and last joist.</p> Solution <pre><code>import cadwork as cw\nimport element_controller as ec\nimport attribute_controller as ac\n\nWIDTH = 60.0\nHEIGHT = 240.0\nSPACING = 625.0\nCOUNT = 10\nLENGTH = 5000.0\n\ny_start = -WIDTH / 2\ny_end = (COUNT - 1) * SPACING + WIDTH / 2\n\n# Rim joist at x=0\np1 = cw.point_3d(0, y_start, 0)\np2 = cw.point_3d(0, y_end, 0)\nlength = p2.distance(p1)\nlength_direction = (p2 - p1).normalized()\nlocal_z_direction = cw.point_3d(0, 0, 1) # Up direction\nrim1 = ec.create_rectangular_beam_vectors(WIDTH, HEIGHT, length, p1, length_direction, local_z_direction)\nac.set_name([rim1], \"Rim Joist\")\n\n# Rim joist at x=LENGTH\np1 = cw.point_3d(LENGTH, y_start, 0)\np2 = cw.point_3d(LENGTH, y_end, 0)\nlength = p2.distance(p1)\nlength_direction = (p2 - p1).normalized()\nlocal_z_direction = cw.point_3d(0, 0, 1) # Up direction\nrim2 = ec.create_rectangular_beam_vectors(WIDTH, HEIGHT, length, p1, length_direction, local_z_direction)\nac.set_name([rim2], \"Rim Joist\")\n\nprint(f\"Created rim joists: {rim1}, {rim2}\")\n</code></pre>"}, {"location": "cadwork-api/exercises-create-modify/#exercise-7-add-blocking-between-joists", "title": "Exercise 7: Add Blocking Between Joists", "text": "<p>Write a script that adds blocking (short cross members) at the midpoint of each joist span. Each blocking element should:</p> <ul> <li>Run along the Y-axis between two adjacent joists</li> <li>Be placed at <code>x = 2500</code> (midpoint of a 5000 mm span)</li> <li>Have a cross-section of <code>60 x 240 mm</code></li> <li>Be named <code>\"Blocking\"</code></li> </ul> <pre><code>flowchart LR\n    subgraph \"Side View at x=2500\"\n        J1[\"Joist 1\"] --- B1[\"Blocking\"] --- J2[\"Joist 2\"] --- B2[\"Blocking\"] --- J3[\"Joist 3\"]\n    end</code></pre> Solution <pre><code>import cadwork as cw\nimport element_controller as ec\nimport attribute_controller as ac\n\nWIDTH = 60.0\nHEIGHT = 240.0\nSPACING = 625.0\nCOUNT = 10\nMID_X = 2500.0\n\ncreated = []\nfor i in range(COUNT - 1):\n    y_start = i * SPACING + WIDTH / 2\n    y_end = (i + 1) * SPACING - WIDTH / 2\n\n    p1 = cw.point_3d(MID_X, y_start, 0)\n    p2 = cw.point_3d(MID_X, y_end, 0)\n    length = p2.distance(p1)\n    length_direction = (p2 - p1).normalized()\n    local_z_direction = cw.point_3d(0, 0, 1) # Up direction\n\n    new_id = ec.create_rectangular_beam_vectors(WIDTH, HEIGHT, length, p1, length_direction, local_z_direction)\n    ac.set_name([new_id], \"Blocking\")\n    ac.set_group([new_id], \"Slab\")\n    ac.set_subgroup([new_id], \"Structure\")\n    created.append(new_id)\n\nprint(f\"Created {len(created)} blocking elements\")\n</code></pre>"}, {"location": "cadwork-api/exercises-create-modify/#exercise-8-modify-cross-sections", "title": "Exercise 8: Modify Cross-Sections", "text": "<p>Write a script that changes the width of all elements named <code>\"Joist\"</code> from <code>60 mm</code> to <code>80 mm</code>.</p> Hint <p>Use <code>gc.set_width_real()</code> to modify the cross-section of an existing element.</p> Solution <pre><code>import element_controller as ec\nimport geometry_controller as gc\nimport attribute_controller as ac\n\nall_ids = ec.get_all_identifiable_element_ids()\njoists = [eid for eid in all_ids if ac.get_name(eid) == \"Joist\"]\n\nfor eid in joists:\n    gc.set_width_real([eid], 80.0)\n\nprint(f\"Updated width for {len(joists)} joists\")\n</code></pre>"}, {"location": "cadwork-api/exercises-create-modify/#exercise-9-duplicate-and-offset-a-slab", "title": "Exercise 9: Duplicate and Offset a Slab", "text": "<p>Write a script that duplicates all elements in the model and moves the copies up by <code>3000 mm</code> along the Z-axis. This simulates creating a second floor.</p> Hint <p>Use <code>ec.copy_elements()</code> and <code>ec.move_element()</code> with a Z-offset vector.</p> Solution <pre><code>import cadwork as cw\nimport element_controller as ec\n\nall_ids = ec.get_all_identifiable_element_ids()\noffset = cw.point_3d(0, 0, 3000)\n\ncopied_ids = ec.copy_elements(all_ids, offset)\n# or copy and then ec.move_element(copied_ids, offset)\n\nprint(f\"Duplicated {len(copied_ids)} elements, offset by Z+3000\")\n</code></pre>"}, {"location": "cadwork-api/exercises-geometry/", "title": "Exercises \u2014 Geometry Data", "text": "<p>These exercises focus on reading geometrical data from the timber framed slab model. You will work with element dimensions, positions, and volumes.</p> <p>Setup</p> <p>Download the timber framed slab model and open it in cadwork 3d. These exercises use <code>element_controller</code>, <code>attribute_controller</code>, and <code>geometry_controller</code>.</p>"}, {"location": "cadwork-api/exercises-geometry/#exercise-1-read-element-dimensions", "title": "Exercise 1: Read Element Dimensions", "text": "<p>Write a script that prints the length, width, and height of each element named <code>\"Joist\"</code>.</p> Hint <p>Use <code>ec.get_length()</code>, <code>ec.get_width()</code>, and <code>ec.get_height()</code> \u2014 these return dimensions in millimeters.</p> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\nimport geometry_controller as gc\n\nall_ids = ec.get_all_identifiable_element_ids()\njoists = [eid for eid in all_ids if ac.get_name(eid) == \"Joist\"]\n\nfor eid in joists:\n    length = gc.get_length(eid)\n    width = gc.get_width(eid)\n    height = gc.get_height(eid)\n    print(f\"ID {eid}: {length:.0f} x {width:.0f} x {height:.0f} mm\")\n</code></pre>"}, {"location": "cadwork-api/exercises-geometry/#exercise-2-calculate-total-volume", "title": "Exercise 2: Calculate Total Volume", "text": "<p>Write a script that calculates the total volume (in m\u00b3) of all elements in the model.</p> <p>Tip</p> <p>cadwork dimensions are in millimeters. To convert mm\u00b3 to m\u00b3, divide by <code>1e9</code>.</p> Solution <pre><code>import element_controller as ec\nimport geometry_controller as gc\n\nall_ids = ec.get_all_identifiable_element_ids()\ntotal_volume_mm3 = sum(\n    gc.get_length(eid) * gc.get_width(eid) * gc.get_height(eid)\n    for eid in all_ids\n)\ntotal_volume_m3 = total_volume_mm3 / 1e9\nprint(f\"Total volume: {total_volume_m3:.3f} m\u00b3\")\n</code></pre>"}, {"location": "cadwork-api/exercises-geometry/#exercise-3-volume-per-material", "title": "Exercise 3: Volume per Material", "text": "<p>Write a script that calculates the total volume (in m\u00b3) grouped by material.</p> <p>Expected output format:</p> <pre><code>GL24h :  1.234 m\u00b3\nC24   :  0.567 m\u00b3\nOSB   :  0.189 m\u00b3\n</code></pre> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\nimport geometry_controller as gc\nfrom collections import defaultdict\n\nall_ids = ec.get_all_identifiable_element_ids()\nvolume_by_material = defaultdict(float)\n\nfor eid in all_ids:\n    material = ac.get_element_material_name(eid)\n    volume_mm3 = gc.get_length(eid) * gc.get_width(eid) * gc.get_height(eid)\n    volume_by_material[material] += volume_mm3\n\nfor material, vol in sorted(volume_by_material.items()):\n    print(f\"{material:25s}: {vol / 1e9:.3f} m\u00b3\")\n\n# or without defaultdict:\nvolume_by_material = {}\nfor eid in all_ids:\n    material = ac.get_element_material_name(eid)\n    volume_mm3 = gc.get_length(eid) * gc.get_width(eid) * gc.get_height(eid)\n    volume_by_material[material] = volume_by_material.get(material, 0) + volume_mm3\nfor material, vol in sorted(volume_by_material.items()):\n    print(f\"{material:25s}: {vol / 1e9:.3f} m\u00b3\")\n</code></pre>"}, {"location": "cadwork-api/exercises-geometry/#exercise-4-find-the-longest-element", "title": "Exercise 4: Find the Longest Element", "text": "<p>Write a script that finds the element with the greatest length and prints its ID, name, and length.</p> Solution <pre><code>import element_controller as ec\nimport geometry_controller as gc\nimport attribute_controller as ac\n\nall_ids = ec.get_all_identifiable_element_ids()\nlongest_id = max(all_ids, key=lambda eid: gc.get_length(eid))\n\nprint(\n    f\"Longest element: ID {longest_id}, \"\n    f\"Name: {ac.get_name(longest_id)}, \"\n    f\"Length: {gc.get_length(longest_id):.0f} mm\"\n)\n</code></pre>"}, {"location": "cadwork-api/exercises-geometry/#exercise-5-element-start-and-end-points", "title": "Exercise 5: Element Start and End Points", "text": "<p>Write a script that prints the start point (P1) and end point (P2) of every joist. Use <code>geometry_controller</code> to access the local coordinate system.</p> Hint <p>Use <code>gc.get_p1(eid)</code> and <code>gc.get_p2(eid)</code> which return <code>point_3d</code> objects with <code>.x</code>, <code>.y</code>, <code>.z</code> attributes.</p> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\nimport geometry_controller as gc\n\nall_ids = ec.get_all_identifiable_element_ids()\njoists = [eid for eid in all_ids if ac.get_name(eid) == \"Joist\"]\n\nfor eid in joists:\n    p1 = gc.get_p1(eid)\n    p2 = gc.get_p2(eid)\n    print(\n        f\"ID {eid}: \"\n        f\"P1({p1.x:.0f}, {p1.y:.0f}, {p1.z:.0f}) -&gt; \"\n        f\"P2({p2.x:.0f}, {p2.y:.0f}, {p2.z:.0f})\"\n    )\n</code></pre>"}, {"location": "cadwork-api/exercises-geometry/#exercise-6-spacing-analysis", "title": "Exercise 6: Spacing Analysis", "text": "<p>Write a script that calculates the center-to-center spacing between adjacent joists. Assume the joists run parallel along the same axis.</p> <pre><code>flowchart LR\n    subgraph Timber Framed Slab\n        J1[\"Joist 1\"] ~~~ J2[\"Joist 2\"] ~~~ J3[\"Joist 3\"] ~~~ J4[\"Joist 4\"]\n    end\n    J1 &lt;--&gt;|\"spacing\"| J2\n    J2 &lt;--&gt;|\"spacing\"| J3\n    J3 &lt;--&gt;|\"spacing\"| J4</code></pre> Hint <p>Get the P1 of each joist, sort by the coordinate perpendicular to the joist direction, then compute differences between consecutive positions. Assumption: Direction of distribution in the Y-axis, so sort by <code>p1.y</code>.</p> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\nimport geometry_controller as gc\n\nall_ids = ec.get_active_identifiable_element_ids() # Select all joists of the slab/floor element\njoists = [eid for eid in all_ids if ac.get_name(eid) == \"Joist\"]\n\n# Get X-coordinate of P1 for each joist (assuming joists run along Y)\njoist_positions: list[tuple[float, int]] = []\nfor eid in joists:\n    p1 = gc.get_p1(eid)\n    joist_positions.append((p1.x, eid))\n\njoist_positions.sort()\n\nprint(\"Joist spacing (center-to-center):\")\nfor i in range(1, len(joist_positions)):\n    spacing = joist_positions[i][0] - joist_positions[i - 1][0]\n    print(\n        f\"  Joist {joist_positions[i-1][1]} -&gt; \"\n        f\"Joist {joist_positions[i][1]}: {spacing:.0f} mm\"\n    )\n</code></pre>"}, {"location": "cadwork-api/exercises-geometry/#exercise-7-geometry-csv-export", "title": "Exercise 7: Geometry CSV Export", "text": "<p>Extend the CSV export from the reading exercises: add columns for <code>Length</code>, <code>Width</code>, <code>Height</code>, <code>Volume_m3</code>, <code>P1_x</code>, <code>P1_y</code>, <code>P1_z</code>.</p> Solution <pre><code>import csv\nimport os\nfrom pathlib import Path\nimport element_controller as ec\nimport attribute_controller as ac\nimport geometry_controller as gc\nimport utility_controller as uc\n\nall_ids = ec.get_all_identifiable_element_ids()\nfile_path = Path(uc.get_3d_file_path()).parent\noutput_path = file_path / \"geometry_report.csv\"\n\nwith open(output_path, \"w\", newline=\"\") as f:\n    writer = csv.writer(f)\n    writer.writerow([\n        \"ID\", \"Name\", \"Material\",\n        \"Length_mm\", \"Width_mm\", \"Height_mm\", \"Volume_m3\",\n        \"P1_x\", \"P1_y\", \"P1_z\",\n    ])\n    for eid in all_ids:\n        p1 = gc.get_p1(eid)\n        length = gc.get_length(eid)\n        width = gc.get_width(eid)\n        height = gc.get_height(eid)\n        volume = (length * width * height) / 1e9\n        writer.writerow([\n            eid,\n            ac.get_name(eid),\n            ac.get_element_material_name(eid),\n            f\"{length:.0f}\",\n            f\"{width:.0f}\",\n            f\"{height:.0f}\",\n            f\"{volume:.4f}\",\n            f\"{p1.x:.1f}\",\n            f\"{p1.y:.1f}\",\n            f\"{p1.z:.1f}\",\n        ])\n\nprint(f\"Exported {len(all_ids)} elements to {output_path}\")\n</code></pre>"}, {"location": "cadwork-api/exercises-reading/", "title": "Exercises \u2014 Reading Model Data", "text": "<p>In these exercises you will work with a timber framed slab model provided as a cadwork 3d file. The goal is to practice querying elements and their attributes without modifying anything.</p> <p>Setup</p> <p>Download the timber framed slab model and open it in cadwork 3d before starting. All exercises use the <code>element_controller</code> and <code>attribute_controller</code> modules.</p>"}, {"location": "cadwork-api/exercises-reading/#exercise-1-count-all-elements", "title": "Exercise 1: Count All Elements", "text": "<p>Write a script that prints the total number of elements in the model.</p> Hint <p>Use <code>ec.get_all_identifiable_element_ids()</code> and <code>len()</code>.</p> Solution <pre><code>import element_controller as ec\n\nall_ids = ec.get_all_identifiable_element_ids()\nprint(f\"Total elements: {len(all_ids)}\")\n</code></pre>"}, {"location": "cadwork-api/exercises-reading/#exercise-2-list-unique-names", "title": "Exercise 2: List Unique Names", "text": "<p>Write a script that collects all element names and prints a sorted list of unique names found in the model.</p> Hint <p>Use a <code>set</code> to collect unique values from <code>ac.get_name()</code>.</p> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\n\nall_ids = ec.get_all_identifiable_element_ids()\nnames = {ac.get_name(eid) for eid in all_ids}\n\nfor name in sorted(names):\n    print(name)\n</code></pre>"}, {"location": "cadwork-api/exercises-reading/#exercise-3-filter-by-name", "title": "Exercise 3: Filter by Name", "text": "<p>Write a script that finds all elements with the name <code>\"Joist\"</code> and prints how many were found.</p> Hint <p>Use a list comprehension with a condition on <code>ac.get_name()</code>.</p> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\n\nall_ids = ec.get_all_identifiable_element_ids()\njoists = [eid for eid in all_ids if ac.get_name(eid) == \"Joist\"]\nprint(f\"Joists found: {len(joists)}\")\n</code></pre>"}, {"location": "cadwork-api/exercises-reading/#exercise-31-filter-by-element-type", "title": "Exercise 3.1: Filter by Element Type", "text": "<p>Write a script that finds all elements of type <code>\"Rectangular Beam\"</code> and prints how many were found.</p> Hint <p>Use <code>ac.get_element_type()</code> to check the type of each element.</p> Solution <pre><code>import cadwork\nimport element_controller as ec\nimport attribute_controller as ac\n\nall_ids = ec.get_all_identifiable_element_ids()\nbeams = [eid for eid in all_ids if ac.get_element_type(eid).is_rectangular_beam()]\nprint(f\"Rectangular Beams found: {len(beams)}\")\n</code></pre>"}, {"location": "cadwork-api/exercises-reading/#exercise-4-group-elements-by-name", "title": "Exercise 4: Group Elements by Name", "text": "<p>Write a script that groups all elements by their name and prints a summary table showing each name and the count of elements.</p> <p>Expected output format:</p> <pre><code>Joist         : 12\nRim Joist     :  4\nSheathing     :  2\nBlocking      :  6\n</code></pre> Hint <p>Use a <code>dict</code> to accumulate counts, or use <code>collections.Counter</code>.</p> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\nfrom collections import Counter\n\nall_ids = ec.get_all_identifiable_element_ids()\nname_counts = Counter(ac.get_name(eid) for eid in all_ids)\n\nfor name, count in sorted(name_counts.items()):\n    print(f\"{name:20s}: {count:3d}\")\n\n# or with a dict:\nname_counts = {}\nfor eid in all_ids:\n    name = ac.get_name(eid)\n    name_counts[name] = name_counts.get(name, 0) + 1\nfor name, count in sorted(name_counts.items()):\n    print(f\"{name:20s}: {count:3d}\")\n</code></pre>"}, {"location": "cadwork-api/exercises-reading/#exercise-5-group-by-name-and-material", "title": "Exercise 5: Group by Name and Material", "text": "<p>Extend the previous exercise: group elements by both name and material, then print a summary.</p> <p>Expected output format:</p> <pre><code>Joist        | GL24h       : 12\nRim Joist    | C24         :  4\nSheathing    | OSB         :  2\n</code></pre> Hint <p>Use a tuple <code>(name, material)</code> as the dictionary key.</p> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\nfrom collections import Counter\n\nall_ids = ec.get_all_identifiable_element_ids()\ngroups = Counter(\n    (ac.get_name(eid), ac.get_element_material_name(eid))\n    for eid in all_ids\n)\n\nfor (name, material), count in sorted(groups.items()):\n    print(f\"{name:15s}| {material:12s}: {count:3d}\")\n</code></pre>"}, {"location": "cadwork-api/exercises-reading/#exercise-6-filter-by-group-and-subgroup", "title": "Exercise 6: Filter by Group and Subgroup", "text": "<p>Write a script that finds all elements where the group is <code>\"Slab\"</code> and the subgroup is <code>\"Structure\"</code>, then prints their names and IDs.</p> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\n\nall_ids = ec.get_all_identifiable_element_ids()\n\nfor eid in all_ids:\n    group = ac.get_group(eid)\n    subgroup = ac.get_subgroup(eid)\n    if group == \"Slab\" and subgroup == \"Structure\":\n        print(f\"ID: {eid}, Name: {ac.get_name(eid)}\")\n</code></pre>"}, {"location": "cadwork-api/exercises-reading/#exercise-61-color-the-joists", "title": "Exercise 6.1: Color the Joists", "text": "<p>Write a script that finds all elements named <code>\"Joist\"</code> and colors them red. Then color all <code>\"Rim Joist\"</code> elements green.</p> Hint <p>Use <code>visualization_controller.set_color(ids, color_id)</code>. Build one list of IDs per name and color each list in a single call.</p> Solution <pre><code>import element_controller as ec\nimport attribute_controller as ac\nimport visualization_controller as vc\n\nall_ids = ec.get_all_identifiable_element_ids()\n\njoists     = [eid for eid in all_ids if ac.get_name(eid) == \"Joist\"]\nrim_joists = [eid for eid in all_ids if ac.get_name(eid) == \"Rim Joist\"]\n\nvc.set_color(joists, 5)\nvc.set_color(rim_joists, 6)\n\nprint(f\"Colored {len(joists)} joists red and {len(rim_joists)} rim joists brown\")\n</code></pre>"}, {"location": "cadwork-api/exercises-reading/#exercise-62-highlight-long-elements", "title": "Exercise 6.2: Highlight Long Elements", "text": "<p>Write a script that selects (not colors) every element longer than <code>4000 mm</code>, so the user can inspect them in the viewport.</p> Hint <p>Use <code>geometry_controller.get_length(eid)</code> and <code>visualization_controller.set_active(ids)</code>.</p> Solution <pre><code>import element_controller as ec\nimport geometry_controller as gc\nimport visualization_controller as vc\n\nall_ids = ec.get_all_identifiable_element_ids()\nlong_ids = [eid for eid in all_ids if gc.get_length(eid) &gt; 4000]\n\nvc.set_active(long_ids)\nprint(f\"Selected {len(long_ids)} long elements\")\n</code></pre>"}, {"location": "cadwork-api/exercises-reading/#exercise-7-export-to-csv", "title": "Exercise 7: Export to CSV", "text": "<p>Write a script that exports a CSV file containing the columns: <code>ID</code>, <code>Name</code>, <code>Group</code>, <code>Subgroup</code>, <code>Material</code> for every element in the model.</p> Hint <p>Use Python's built-in <code>csv</code> module and <code>utility_controller.get_3d_file_path()</code> for the output location.</p> Solution <pre><code>import csv\nimport os\nimport element_controller as ec\nimport attribute_controller as ac\nimport utility_controller as uc\n\nall_ids = ec.get_all_identifiable_element_ids()\nfile_path = Path(uc.get_3d_file_path()).parent\noutput_path = file_path / \"element_report.csv\"\n\nwith open(output_path, \"w\", newline=\"\") as f:\n    writer = csv.writer(f)\n    writer.writerow([\"ID\", \"Name\", \"Group\", \"Subgroup\", \"Material\"])\n    for eid in all_ids:\n        writer.writerow([\n            eid,\n            ac.get_name(eid),\n            ac.get_group(eid),\n            ac.get_subgroup(eid),\n            ac.get_element_material_name(eid),\n        ])\n\nprint(f\"Exported {len(all_ids)} elements to {output_path}\")\n</code></pre>"}, {"location": "cadwork-api/geometry/", "title": "Geometry \u2014 Linear Algebra in 3D", "text": "<p>Almost every cadwork API call that creates or moves elements takes points and vectors as arguments. This page is a short refresher on the linear algebra you need: what a point is, what a vector is, how the two interact, and how the unit circle turns angles into directions.</p> <p>In the cadwork Python API, both points and vectors are represented by the same type, <code>cadwork.point_3d</code>. The mathematical meaning (position vs. direction) lives in your head, not in the type \u2014 keeping that distinction sharp will save you many bugs.</p>"}, {"location": "cadwork-api/geometry/#coordinate-space-right-handed", "title": "Coordinate space (right-handed)", "text": ""}, {"location": "cadwork-api/geometry/#cadwork-elements-local-coordinate-system-lcs", "title": "cadwork elements - Local Coordinate System (LCS)", "text": "<p>Every element has its own local coordinate system (LCS) that defines how it is oriented in 3D space. The LCS has three axes:</p> <ul> <li>Length axis: The primary axis along the length of the element (e.g., along a beam or joist).</li> <li>Width axis: The secondary axis that defines the width of the element (e.g., across a beam or joist).</li> <li>Height axis: The tertiary axis that defines the height of the element (e.g., vertical direction for a beam or joist).</li> </ul> <p></p>"}, {"location": "cadwork-api/geometry/#points", "title": "Points", "text": "<p>A point is a position in 3D space. It answers the question where? \u2014 for example, the axis start of a joist.</p> <pre><code>import cadwork as cw\n\np = cw.point_3d(1000, 500, 0)   # x, y, z in millimeters\nprint(p.x, p.y, p.z)\n</code></pre>"}, {"location": "cadwork-api/geometry/#translating-a-point-point-vector", "title": "Translating a point (point + vector)", "text": "<p>Adding a vector to a point shifts it by that displacement. The result is a new point.</p> <pre><code>import cadwork as cw\n\norigin = cw.point_3d(0, 0, 0)\noffset = cw.point_3d(625, 0, 0)   # used as a vector here\nmoved  = origin + offset          # cw.point_3d(625, 0, 0)\n# (origin.x + offset.x, origin.y + offset.y, origin.z + offset.z)\n</code></pre> <p>This is exactly how Exercise 5 in Create &amp; Modify places joists at <code>y = i * 625</code> \u2014 repeated translation along the Y-axis.</p>"}, {"location": "cadwork-api/geometry/#distance-between-two-points", "title": "Distance between two points", "text": "<pre><code>import cadwork as cw\n\np1 = cw.point_3d(0, 0, 0)\np2 = cw.point_3d(5000, 0, 0)\n\nlength = p2.distance(p1)   # 5000.0\n</code></pre> <p>Internally this is just <code>sqrt((p2.x - p1.x)**2 + (p2.y - p1.y)**2 + (p2.z - p1.z)**2)</code>.</p>"}, {"location": "cadwork-api/geometry/#midpoint-between-two-points", "title": "Midpoint between two points", "text": "<p>The midpoint is the average of the two coordinates:</p> <pre><code>mid = cw.point_3d(\n    (p1.x + p2.x) / 2,\n    (p1.y + p2.y) / 2,\n    (p1.z + p2.z) / 2,\n)\n</code></pre> <p>This is a common building block \u2014 for example, when placing blocking at the center of a joist span.</p>"}, {"location": "cadwork-api/geometry/#vectors", "title": "Vectors", "text": ""}, {"location": "cadwork-api/geometry/#interpreting-a-vector-as-a-sequence-of-displacements", "title": "Interpreting a vector as a sequence of displacements", "text": "<p>A vector has a direction and a magnitude (length). It answers the question how far, and which way? \u2014 for example, \"from P1 to P2\" or \"5000 mm along +X\".</p> <p>The key idea: point \u2212 point = vector. Subtracting two points gives the displacement from one to the other.</p> <pre><code>import cadwork as cw\n\np1 = cw.point_3d(0, 0, 0)\np2 = cw.point_3d(5000, 0, 0)\n\ndirection = p2 - p1   # vector pointing from p1 to p2\n# direction = cw.point_3d(p2.x - p1.x, p2.y - p1.y, p2.z - p1.z)\n</code></pre>"}, {"location": "cadwork-api/geometry/#length-magnitude-of-a-vector", "title": "Length (magnitude) of a vector", "text": "<p>The length of a vector <code>v = (x, y, z)</code> is <code>sqrt(x\u00b2 + y\u00b2 + z\u00b2)</code>. For a vector built from <code>p2 - p1</code>, this equals <code>p2.distance(p1)</code>.</p>"}, {"location": "cadwork-api/geometry/#normalizing-making-a-unit-vector", "title": "Normalizing \u2014 making a unit vector", "text": "<p>A unit vector has length 1. It carries direction only, no magnitude. You normalize a vector by dividing each component by its length:</p> <pre><code>length_direction = (p2 - p1).normalized()\n</code></pre> <p>This is the 5th argument to <code>ec.create_rectangular_beam_vectors(width, height, length, p1, length_direction, local_z_direction)</code>: cadwork wants a direction, not a \"to\" point, so the magnitude must be stripped away.</p> <p>Don't normalize the zero vector</p> <p><code>(p1 - p1).normalized()</code> divides by zero. If you compute a direction from two points, make sure they are distinct.</p>"}, {"location": "cadwork-api/geometry/#adding-and-scaling-vectors", "title": "Adding and scaling vectors", "text": "<p>Vectors can be added (combine two displacements) and scaled (stretch or shrink a displacement). Scaling a unit vector by a length gives you a vector of exactly that length.</p> <pre><code>direction = (p2 - p1).normalized()       # length 1\noffset_2m = cw.point_3d(direction.x * 2000,\n                        direction.y * 2000,\n                        direction.z * 2000)\nmidpoint  = p1 + cw.point_3d(direction.x * 2500,\n                             direction.y * 2500,\n                             direction.z * 2500)\n</code></pre> <p>Pattern: start point + (unit direction \u00d7 distance) = point at that distance along the direction. This is how you place an element 2.5 m along an arbitrary axis without caring whether the axis is X, Y, or diagonal.</p> <pre><code>origin = cw.point_3d(0, 0, 0)\ndirection = cw.point_3d(1., 0., 0.) # along global X axis\nlength = 2500.0\np = origin + direction * length\n# what happens behind the operator overload: p = cw.point_3d(\n#     origin.x + direction.x * length,\n#     origin.y + direction.y * length,\n#     origin.z + direction.z * length,\n# )\n</code></pre> <pre><code>            | origin.x |        | direction.x |\norigin   =  | origin.y |   d =  | direction.y |\n            | origin.z |        | direction.z |\n\np = origin + d * length\n\n    | origin.x + direction.x * length |\np = | origin.y + direction.y * length |\n    | origin.z + direction.z * length |\n\nWith origin = (0, 0, 0), direction = (1, 0, 0), length = 2500:\n\n    | 0 + 1 * 2500 |   | 2500 |\np = | 0 + 0 * 2500 | = |    0 |\n    | 0 + 0 * 2500 |   |    0 |\n</code></pre>"}, {"location": "cadwork-api/geometry/#dot-product-angle-and-projection", "title": "Dot product \u2014 angle and projection", "text": "<p>The dot product <code>a \u00b7 b = a.x*b.x + a.y*b.y + a.z*b.z</code> collapses two vectors into a single number. Its sign and magnitude tell you how the vectors relate:</p> Dot product of unit vectors Meaning <code>1</code> same direction (parallel) <code>0</code> perpendicular <code>-1</code> opposite direction <p></p> <p>For unit vectors, <code>a \u00b7 b = cos(angle between them)</code>. So to find the angle:</p> <pre><code>import math\n\ndef dot(a, b):\n    return a.x * b.x + a.y * b.y + a.z * b.z\n\ncos_theta = dot(u1, u2)              # u1, u2 already normalized\nangle_rad = math.acos(cos_theta)     # cos takes an angle and returns a ratio (a number between -1 and 1).\n# acos takes a ratio and returns an angle (in radians).\nangle_deg = math.degrees(angle_rad)\n</code></pre>"}, {"location": "cadwork-api/geometry/#projection", "title": "Projection", "text": "<p>The dot product also gives you the length of the projection of <code>a</code> onto <code>b</code>:</p> <pre><code>a = cw.point_3d(1, 2, 3)\nb = cw.point_3d(0, 1, 0)\nprojection_length = dot(a, b.normalized())\n</code></pre> <p></p> <p>Use it to check whether two beams are parallel, perpendicular, or to filter elements by orientation.</p>"}, {"location": "cadwork-api/geometry/#distance-from-a-point-to-a-line", "title": "Distance from a point to a line", "text": "<p>The distance from a point <code>p</code> to the line through <code>p1</code> in direction <code>d</code> is the length of the projection of <code>p - p1</code> onto the plane perpendicular to <code>d</code>:</p> <pre><code>def distance_point_to_line(p: cw.point_3d, p1: cw.point_3d, d: cw.point_3d) -&gt; float:\n    v = p - p1\n    projection_length = dot(v, d.normalized()) # t (t = how far along the line the projection falls)\n    projection = cw.point_3d(d.x * projection_length,\n                            d.y * projection_length,\n                            d.z * projection_length)\n                            # projection = p + t * d\n    rejection = v - projection # perpendicular from p to the line\n    return rejection.length()\n</code></pre>"}, {"location": "cadwork-api/geometry/#distance-from-a-point-to-a-plane", "title": "Distance from a point to a plane", "text": "<p>The distance from a point <code>p</code> to the plane through <code>p1</code> (plane origin) with normal <code>n</code> is the length of the projection of <code>p - p1</code> onto <code>n</code>:</p> <pre><code>def distance_point_to_plane(p: cw.point_3d, p1: cw.point_3d, n: cw.point_3d) -&gt; float:\n    v = p - p1\n    projection_length = dot(v, n.normalized()) # t (t = how far along the normal the projection falls)\n    projection = cw.point_3d(n.x * projection_length,\n                            n.y * projection_length,\n                            n.z * projection_length)\n                            # projection = p + t * n\n    return projection.length()\n</code></pre>"}, {"location": "cadwork-api/geometry/#cross-product-perpendicular-direction", "title": "Cross product \u2014 perpendicular direction", "text": "<p>The cross product <code>a \u00d7 b</code> returns a new vector perpendicular to both <code>a</code> and <code>b</code>. It is how you build local coordinate systems: given a length axis and an \"up\" hint, the cross product gives you the side axis.</p> <pre><code>def cross(a: cw.point_3d, b: cw.point_3d) -&gt; cw.point_3d:\n    return cw.point_3d(\n        a.y * b.z - a.z * b.y,\n        a.z * b.x - a.x * b.z,\n        a.x * b.y - a.y * b.x,\n    )\n\nlength_axis = (p2 - p1).normalized()\nup_hint     = cw.point_3d(0, 0, 1)\nside_axis   = cross(length_axis, up_hint)\n</code></pre>"}, {"location": "cadwork-api/geometry/#point-vs-vector-the-mental-model", "title": "Point vs. Vector \u2014 the mental model", "text": "<p>Same type in code, different meanings in math:</p> Concept Answers Example Operations that make sense Point where? start of a joist point \u2212 point, point + vector Vector how/where to? \"along +X by 5000 mm\" add, scale, normalize, dot, cross <p>Two rules of thumb:</p> <ul> <li>Point + Point is meaningless (averaging coordinates is fine for a midpoint, but adding \"two locations\" has no geometric meaning).</li> <li>Vector + Vector, Point + Vector, and Point \u2212 Point are the operations you actually use.</li> </ul>"}, {"location": "cadwork-api/geometry/#the-unit-circle", "title": "The Unit Circle", "text": "<p>The unit circle is the circle of radius 1 centered at the origin. Every point on it can be written as <code>(cos \u03b8, sin \u03b8)</code>, where <code>\u03b8</code> is the angle measured counter-clockwise from the +X axis.</p> <p></p> <p>This single fact \u2014 angle \u2194 point on the unit circle \u2014 is the bridge between angles and directions. Whenever you need to \"go in some direction\" or \"rotate something\", the unit circle is doing the work behind the scenes.</p>"}, {"location": "cadwork-api/geometry/#1-building-a-direction-vector-from-an-angle", "title": "1. Building a direction vector from an angle", "text": "<p>To create a horizontal unit vector pointing at angle <code>\u03b8</code> (in the XY plane):</p> <pre><code>import math\nimport cadwork as cw\n\nangle_deg = 30\ntheta = math.radians(angle_deg)\n\ndirection = cw.point_3d(math.cos(theta), math.sin(theta), 0)\n</code></pre> <p>Multiply by a length to get a displacement, then add to a start point \u2014 and you have placed a beam at any angle without computing X and Y by hand.</p>"}, {"location": "cadwork-api/geometry/#2-distributing-elements-around-a-circle", "title": "2. Distributing elements around a circle", "text": "<p>Want N posts evenly spaced around a center point? Step <code>\u03b8</code> from <code>0</code> to <code>2\u03c0</code> in <code>N</code> increments and read off the unit circle:</p> <pre><code>import math\nimport cadwork as cw\nimport element_controller as ec\n\ncenter = cw.point_3d(0, 0, 0)\nradius = 3000\ncount  = 8\n\nfor i in range(count):\n    theta = 2 * math.pi * i / count\n    p1 = cw.point_3d(\n        center.x + radius * math.cos(theta),\n        center.y + radius * math.sin(theta),\n        center.z,\n    )\n    p2 = cw.point_3d(p1.x, p1.y, p1.z + 2500)   # vertical post, 2.5 m tall\n    # ec.create_rectangular_beam_vectors...\n</code></pre> <p>This is the classic \"place columns around a gazebo\" pattern \u2014 and it generalizes to any radial layout (fasteners around a connector, light fixtures around a hall, etc.).</p>"}, {"location": "cadwork-api/geometry/#3-rotating-a-2d-point-around-the-origin", "title": "3. Rotating a 2D point around the origin", "text": "<p>Rotating the point <code>(x, y)</code> by an angle <code>\u03b8</code> is a direct application of sine and cosine:</p> <pre><code>def rotate_xy(p, theta):\n    return cw.point_3d(\n        p.x * math.cos(theta) - p.y * math.sin(theta),\n        p.x * math.sin(theta) + p.y * math.cos(theta),\n        p.z,\n    )\n</code></pre> <p>To rotate around an arbitrary center <code>c</code>, translate to the origin, rotate, translate back:</p> <pre><code>def rotate_around(p, center, theta):\n    shifted = cw.point_3d(p.x - center.x, p.y - center.y, p.z - center.z)\n    rotated = rotate_xy(shifted, theta)\n    return cw.point_3d(rotated.x + center.x,\n                       rotated.y + center.y,\n                       rotated.z + center.z)\n</code></pre>"}, {"location": "cadwork-api/geometry/#4-recovering-an-angle-from-a-direction", "title": "4. Recovering an angle from a direction", "text": "<p>The inverse of <code>(cos \u03b8, sin \u03b8)</code> is <code>atan2(y, x)</code>. Use <code>math.atan2</code> (not <code>atan</code>) \u2014 it handles all four quadrants and the edge cases where <code>x = 0</code>.</p> <pre><code>import math\n\ndirection = (p2 - p1).normalized()\nangle_rad = math.atan2(direction.y, direction.x)\nangle_deg = math.degrees(angle_rad)\n</code></pre> <p>This is how you answer \"what angle does this beam make with the X-axis?\" \u2014 useful when sorting elements by orientation or labeling them by compass direction.</p>"}, {"location": "cadwork-api/geometry/#summary", "title": "Summary", "text": "<ul> <li>A point is a where; a vector is a how far / which way.</li> <li><code>point \u2212 point</code> gives a vector. <code>point + vector</code> gives a point. <code>vector.normalized()</code> keeps the direction, drops the length.</li> <li>Build positions with the pattern start + (unit direction \u00d7 distance) \u2014 it works for any axis, not just X / Y / Z.</li> <li>The unit circle turns angles into directions via <code>(cos \u03b8, sin \u03b8)</code>. Use it for radial layouts, rotations, and recovering angles with <code>atan2</code>.</li> </ul> <p>With these few operations you can already express every placement and movement task in the Geometry and Create &amp; Modify exercises.</p>"}, {"location": "cadwork-api/getting-started/", "title": "Getting Started", "text": "<p>This page explains how to set up and run Python scripts within cadwork 3d.</p>"}, {"location": "cadwork-api/getting-started/#python-in-cadwork-3d", "title": "Python in cadwork 3d", "text": "<p>cadwork 3d includes an embedded Python interpreter. Scripts can be executed directly from within the application to automate tasks such as creating elements, modifying attributes, or generating reports.</p>"}, {"location": "cadwork-api/getting-started/#running-a-script", "title": "Running a Script", "text": "<p>Option 1: Use the IDLE Shell (Integrated Development and Learning Environment) that comes with Python. You can write and execute your script in the IDLE editor, which provides a simple interface for running Python code.</p> <p></p> <p>Option 2: Save your script as a <code>.py</code> file in the <code>API.x64</code> folder of your cadwork user profile. Then, you can execute it from the plugin bar in cadwork 3d. 1. Open cadwork 3d 2. Navigate to the Userprofil <code>..\\cadwork\\userprofil_2026\\3d\\API.x64</code> 3. Create a folder named <code>my_script</code> and save your Python script there (<code>my_script.py</code>) The python file must hold the same name as the folder. 4. Start cadwork 3d and execute the script from the plugin bar</p> <p></p>"}, {"location": "cadwork-api/getting-started/#first-script", "title": "First Script", "text": "<p>A minimal cadwork Python script:</p> <pre><code>import cadwork as cw\nimport element_controller as ec\n\n# Get all element IDs in the model\nelement_ids = ec.get_all_identifiable_element_ids()\nprint(f\"Number of elements: {len(element_ids)}\")\n</code></pre>"}, {"location": "cadwork-api/getting-started/#api-modules", "title": "API Modules", "text": "<pre><code>flowchart TD\n    CW[\"cadwork 3d Python API\"] --&gt; EC[\"element_controller\"]\n    CW --&gt; AC[\"attribute_controller\"]\n    CW --&gt; GC[\"geometry_controller\"]\n    CW --&gt; VC[\"visualization_controller\"]\n    CW --&gt; UC[\"utility_controller\"]\n    CW --&gt; CORE[\"cadwork (core)\"]\n\n    EC --- EC_DESC[\"Create, delete, query elements\"]\n    AC --- AC_DESC[\"Read and write attributes\"]\n    GC --- GC_DESC[\"Access element geometry\"]\n    VC --- VC_DESC[\"Visibility and colors\"]\n    UC --- UC_DESC[\"prompts, helpers\"]\n\n    style EC_DESC fill:none,stroke:none\n    style AC_DESC fill:none,stroke:none\n    style GC_DESC fill:none,stroke:none\n    style VC_DESC fill:none,stroke:none\n    style UC_DESC fill:none,stroke:none</code></pre> <p>The cadwork Python API is organized into several modules:</p> Module Description <code>cadwork</code> Core utilities and types <code>element_controller</code> Create, delete, and query elements <code>attribute_controller</code> Read and write element attributes <code>geometry_controller</code> Access element geometry <code>visualization_controller</code> Control visibility and colors <code>utility_controller</code> File dialogs, user prompts, and helpers"}, {"location": "cadwork-api/mcp/", "title": "AI-Assisted Scripting &amp; MCP", "text": "<p>In practice, most of the cadwork Python you will write after this workshop will be drafted with the help of an AI assistant (Claude, ChatGPT, GitHub Copilot, \u2026). This page shows you how to do that properly \u2014 and what Cadwork API MCP is, for when you are ready to go deeper.</p>"}, {"location": "cadwork-api/mcp/#why-use-ai-to-write-cadwork-scripts", "title": "Why use AI to write cadwork scripts", "text": "<p>Writing a cadwork script is two jobs in one.</p> <p>You are already good at: - Defining the engineering problem - Recognizing whether a result is right</p> <p>You may not want to spend time on: - Remembering every API function name - Getting the argument order right - Gluing three controllers into a thirty-line script</p> <p>AI handles the second list. Modern models have read more of Python code and more example scripts than any one engineer ever will. You stay in charge of the first.</p> <p>Think of the AI as a fluent coder who knows the API cold but has never seen your project. You bring the engineering intent. It writes the code. You verify it.</p>"}, {"location": "cadwork-api/mcp/#the-iteration-loop", "title": "The iteration loop", "text": "<pre><code>flowchart LR\n    A[\"Describe the task&lt;br/&gt;in plain language\"] --&gt; B[\"AI drafts a script\"]\n    B --&gt; C[\"Run it in cadwork\"]\n    C --&gt; D{\"Did it work?\"}\n    D --&gt;|\"Yes\"| E[\"Done\"]\n    D --&gt;|\"No\"| F[\"Paste the error&lt;br/&gt;back to the AI\"]\n    F --&gt; B</code></pre> <p>Three rounds is usually enough for a workshop-scale script.</p> <p>Here's some rules and important tips to use these tools effectively.</p>"}, {"location": "cadwork-api/mcp/#user-instructions-writing-a-good-prompt", "title": "User instructions: Writing a good prompt", "text": "<p>Bad prompt:</p> <p>Write a cadwork script that makes joists.</p> <p>Good prompt:</p> <p>I have a cadwork 3d model open with a single rim joist selected. Write a Python script that:</p> <ul> <li>Reads the selected rim joist's length and direction</li> <li>Creates joists perpendicular to it, every 625 mm, with a 60\u00d7240 mm cross-section, 5000 mm long</li> <li>Names each joist \"Joist\", group \"Slab\", subgroup \"Structure\"</li> <li>Colors them red (color id 3) for visual confirmation</li> </ul> <p>Optionally, if you want to be more specific about the API usage, you can add:</p> <p>Use these modules: <code>element_controller as ec</code>, <code>attribute_controller as ac</code>, <code>geometry_controller as gc</code>, <code>visualization_controller as vc</code>. <code>create_rectangular_beam_vectors(width, height, length, p1, xl_direction, zl_direction)</code> is the function for beam creation.</p> <p>The good prompt:</p> <ul> <li>States the starting condition (what's selected, what's open)</li> <li>Lists the steps in order</li> <li>Defines success (color them so I can see it worked)</li> <li>(Optional) Names the exact API functions and modules to use</li> </ul>"}, {"location": "cadwork-api/mcp/#context-rotting-keep-it-short", "title": "Context rotting: keep it short", "text": "<p>The AI has a limited context window (the amount of text it can \"see\" at once). If your conversation with the AI gets too long, its generation quality will degrade. This is called context rotting.</p> <p></p> <p>For small scripts, 3 rounds of iteration is usually enough. If you find yourself doing 10 rounds of back-and-forth with the AI, it's probably time to start a new conversation and copy-paste the working code into it.</p> <p>For larger scripts, split your task in at least two:</p> <ul> <li>plan: produce a high-level outline of the script but no code yet</li> <li>code: write the actual code, referring to the plan for guidance</li> </ul> <p></p>"}, {"location": "cadwork-api/mcp/#reflexion-when-the-ai-gets-it-wrong", "title": "Reflexion: when the AI gets it wrong", "text": "<p>Paste the full traceback error back into the conversation:</p> <pre><code>Traceback (most recent call last):\n  File \"frame.py\", line 12, in &lt;module&gt;\n    ec.create_rectangular_beam_vectors(60, 240, p1, p2)\nTypeError: create_rectangular_beam_vectors() takes 6 positional arguments but 4 were given\n</code></pre> <p>Please fix the function call. The signature is <code>(width, height, length, p1, xl_direction, zl_direction)</code>.</p> <p>Give the model the error verbatim plus the correct signature. It will fix the call. Do not summarize the error or say \"the error is that you forgot the length argument\". The exact error message and signature are what the model needs to fix the code.</p>"}, {"location": "cadwork-api/mcp/#cadwork-specific-context-model-context-protocol-mcp", "title": "Cadwork-specific context: Model Context Protocol (MCP)", "text": ""}, {"location": "cadwork-api/mcp/#quickstart", "title": "Quickstart", "text": "<p>Add the following to your VS Code settings file (.vscode/mcp.json in your workspace or user settings):</p> <ol> <li>Install the MCP server:</li> </ol> <p><pre><code>{\n  \"servers\": {\n    \"cadwork-pyapi\": {\n      \"type\": \"http\",\n      \"url\": \"https://pyapi.mcp.cadwork.dev/mcp\"\n    }\n  }\n}\n</code></pre> For other IDE or AI Assistatnts, refer to the MCP docs, here</p>"}, {"location": "cadwork-api/mcp/#cadwork-context-aware-ai-assistant", "title": "Cadwork context-aware AI assistant", "text": "<p>Any AI has not necessarily been trained on the cadwork API, and for the AI to search documentation is still expensive in terms of time and tokens. That is why the AI can get function signatures wrong, or suggest non-existent functions.</p> <p>The Cadwork Python API Model Context Protocol (MCP) injects correct Cadwork API Python function signatures and other knowledge into the AI's environment.</p> <pre><code>flowchart LR\n    A[\"AI client&lt;br/&gt;(e.g. Claude)\"] &lt;--&gt;|\"MCP&lt;br/&gt;(JSON-RPC)\"| B[\"MCP server&lt;br/&gt;(Python)\"]\n    B &lt;--&gt;|\"cadwork&lt;br/&gt;Python API&lt;br/&gt;and knowledge\"| C[\"cadwork 3d\"]</code></pre> <p>An MCP server exposes cadwork API knowledge as tools the AI can search directly. Take it as a very fast Cadwork search engine for AI.</p> <p>To this day <code>19.05.2026</code>, the MCP Cadwork gives access to the following tools:</p> <ul> <li><code>search_api_examples</code>: search for code examples by keyword</li> <li><code>search_api_signatures</code>: search for function signatures by keyword</li> <li><code>search_api_modules</code>: search for module contents by keyword</li> <li><code>search_community_threads</code>: search for community forum threads by keyword</li> </ul> <p>Do not worry, you don't need to use these tools, your AI agent will decide when to use them. You just need to set up the server and connect it to your AI client.</p> <p>Use MCP to learn the API faster</p> <p>If you are new to the API, using an MCP server can help you learn it faster. The AI will pull in the correct function signatures and examples, so you can see how to use the API in context.  Type prompts like <code>\"How do I create a rectangular beam?\"</code> and see the AI pull in the exact function signature and example code.</p> <p>MCP vs documentation</p> <p>MCP is not a replacement for documentation. It is a complement that gives the AI direct access to the API knowledge it needs to write code. You still need to understand the API and read the docs to debug it effectively.</p>"}, {"location": "cadwork-api/mcp/#further-reading", "title": "Further reading", "text": "<ul> <li>Model Context Protocol specification</li> <li>Claude Code \u2014 terminal-based AI that supports MCP servers out of the box</li> </ul> <p>Workshop takeaway</p> <p>Treat the AI as a fast pair-programmer who knows Python well and cadwork roughly. You bring the engineering judgment, the model context, and the visual verification. The AI brings the syntax and the boilerplate.</p>"}, {"location": "cadwork-api/model-download/", "title": "Exercise Model \u2014 Download", "text": "<p>All cadwork 3d API exercises on this site use the same model: a timber framed slab with joists, rim joists, blocking, and sheathing. Download it once, then open it in cadwork 3d before starting the exercises.</p>"}, {"location": "cadwork-api/model-download/#download", "title": "Download", "text": "<p>\u2b07 timber-framed-elements.3d</p> <p>Timber framed slab \u2014 cadwork 3d file (open in cadwork 3d v2025 or newer)</p> <p>How to use the file</p> <ol> <li>Click the link above and save the file to your local machine.</li> <li>Open it in cadwork 3d (double-click the file).</li> <li>Keep a copy of the original \u2014 some exercises modify the model and you may want to reset.</li> </ol>"}, {"location": "cadwork-api/model-download/#whats-inside-the-model", "title": "What's inside the model", "text": ""}, {"location": "cadwork-api/visualization/", "title": "Visualization", "text": "<p>The <code>visualization_controller</code> lets your script give visible feedback in the cadwork 3d viewport. Color elements, hide them, change the camera, or programmatically select a subset \u2014 perfect for confirming that a filter or generator did what you expected.</p>"}, {"location": "cadwork-api/visualization/#coloring-elements", "title": "Coloring elements", "text": "<pre><code>import element_controller as ec\nimport attribute_controller as ac\nimport visualization_controller as vc\n\nall_ids = ec.get_all_identifiable_element_ids()\njoists = [eid for eid in all_ids if ac.get_name(eid) == \"Joist\"]\n\nvc.set_color(joists, 3)   # color id 3 = red in the default palette\n</code></pre> <p>Color IDs are integers referring to the cadwork color palette.</p> <p>Tip</p> <p>Open the cadwork color dialog to see the full palette and the ID assigned to each swatch.</p>"}, {"location": "cadwork-api/visualization/#programmatic-selection", "title": "Programmatic selection", "text": "<pre><code>import visualization_controller as vc\n\nvc.set_active(joists)   # selects the elements in the viewport\n</code></pre> <p>The user sees the joists highlighted as if they had clicked them. Useful when your script identifies a subset and you want the user to inspect them next.</p>"}, {"location": "cadwork-api/visualization/#visibility", "title": "Visibility", "text": "<pre><code>import visualization_controller as vc\n\nvc.set_invisible(insulation_ids)   # remove from view\nvc.set_visible(insulation_ids)   # bring back\nvc.show_all_elements()             # reset\n</code></pre> <p>Hide insulation while you work on the structural frame, then bring it back at the end.</p>"}, {"location": "cadwork-api/visualization/#camera-views", "title": "Camera views", "text": "<pre><code>import visualization_controller as vc\n\nvc.show_view_positive_x()   # look along +X\nvc.show_view_positive_y()   # look along +Y\nvc.show_view_positive_z()   # top view\nvc.show_view_standard_axo() # standard isometric\n</code></pre> <p>Set the view at the start of a script so screenshots come out consistent.</p>"}, {"location": "cadwork-api/visualization/#worked-example-color-by-material", "title": "Worked example \u2014 color by material", "text": "<pre><code>import element_controller as ec\nimport attribute_controller as ac\nimport visualization_controller as vc\n\nCOLOR_BY_MATERIAL = {\n    \"GL24h\": 3,   \n    \"GL28h\": 4,   \n    \"C24\":   5,   \n    \"OSB\":   6,   \n}\n\nall_ids = ec.get_all_identifiable_element_ids()\n\nfor eid in all_ids:\n    material = ac.get_element_material_name(eid)\n    color = COLOR_BY_MATERIAL.get(material)\n    if color is not None:\n        vc.set_color([eid], color)\n</code></pre> <p>Every element with a known material gets a distinct color. Elements with unknown materials keep their original color (because <code>dict.get</code> returns <code>None</code> and we skip them).</p> <p>Quick Check: Color by length</p> <p>Write the loop body to color long beams (length &gt; 5000 mm) red (<code>3</code>) and short beams green (<code>4</code>). Assume <code>lengths[eid]</code> is already populated.</p> <pre><code>??? success \"Show answer\"\n    ```python\n    for eid, length in lengths.items():\n        if length &gt; 5000:\n            vc.set_color([eid], 3)\n        else:\n            vc.set_color([eid], 4)\n    ```\n\n    Or in one pass without a dict:\n\n    ```python\n    for eid in all_ids:\n        length = gc.get_length(eid)\n        vc.set_color([eid], 3 if length &gt; 5000 else 4)\n    ```\n</code></pre> <p>Quick Check: Selection vs. color</p> <p>What is the difference between <code>vc.set_color(ids, 3)</code> and <code>vc.set_active(ids)</code>?</p> <pre><code>??? success \"Show answer\"\n    - `set_color` **changes** an element's color attribute \u2014 the change persists until you set it again (or undo).\n    - `set_active` only **selects** the elements in the viewport \u2014 like clicking on them. No attribute is modified.\n\n    Use color when you want a lasting visual mark. Use selection when you want the user to \"be in\" that subset (e.g., to apply a manual operation next).\n</code></pre>"}, {"location": "python-basics/", "title": "Python Basics", "text": "<p>This section is a refresher on Python fundamentals. Each topic mixes short explanations, code examples, and Quick Check questions you can answer before revealing the solution.</p>"}, {"location": "python-basics/#how-to-use-this-section", "title": "How to use this section", "text": "<p>Each page contains blocks like this:</p> <p>Quick Check</p> <p>Try to answer first, then click the toggle below.</p> Show answer <p>Answers appear here when you click.</p> <p>Work through them in order \u2014 concepts build on each other.</p>"}, {"location": "python-basics/#topics", "title": "Topics", "text": "<ul> <li>Data Types \u2014 Variables, numbers, strings, lists, and dictionaries</li> <li>Data Structures \u2014 Lists, tuples, sets, and dictionaries in depth</li> <li>Control Flow \u2014 Conditional statements and loops</li> <li>Functions \u2014 Defining and using functions and modules</li> </ul> <p>For lecturers</p> <p>Use the Quick Check questions live: read the question aloud, give students 30\u201360 seconds, collect answers, then reveal.</p>"}, {"location": "python-basics/control-flow/", "title": "Control Flow", "text": "<p>Control flow statements let you execute code conditionally or repeatedly.</p> <p></p>"}, {"location": "python-basics/control-flow/#if-else", "title": "If / Else", "text": "<pre><code>flowchart TD\n    A[\"length = 5000\"] --&gt; B{\"length &gt; 6000?\"}\n    B --&gt;|Yes| C[\"Long element\"]\n    B --&gt;|No| D{\"length &gt; 3000?\"}\n    D --&gt;|Yes| E[\"Medium element\"]\n    D --&gt;|No| F[\"Short element\"]</code></pre> <pre><code>length = 5000\n\nif length &gt; 6000:\n    print(\"Long element\")\nelif length &gt; 3000:\n    print(\"Medium element\")\nelse:\n    print(\"Short element\")\n</code></pre> <p>Quick Check: Branching</p> <p>For each value of <code>length</code>, which branch executes?</p> length branch 7500 ? 6000 ? 3001 ? 3000 ? Show answer length branch 7500 Long element 6000 Medium element 3001 Medium element 3000 Short element <p>Watch the boundaries: <code>&gt;</code> is strict, so <code>6000 &gt; 6000</code> is <code>False</code>. Use <code>&gt;=</code> if you want to include the boundary.</p>"}, {"location": "python-basics/control-flow/#for-loops", "title": "For Loops", "text": "<pre><code>flowchart TD\n    A[\"Start\"] --&gt; B[\"Get next item\"]\n    B --&gt; C{\"Items remaining?\"}\n    C --&gt;|Yes| D[\"Execute loop body\"]\n    D --&gt; B\n    C --&gt;|No| E[\"End\"]</code></pre> <p>Iterate over sequences:</p> <pre><code>materials = [\"GL24h\", \"C24\", \"BSH\"]\n\nfor material in materials:\n    print(f\"Material: {material}\")\n</code></pre> <p>Iterate with index using <code>enumerate</code>:</p> <pre><code>for i, material in enumerate(materials):\n    print(f\"{i}: {material}\")\n</code></pre> <p>Quick Check: Loop output</p> <p>What is printed by this loop?</p> <pre><code>lengths = [2000, 5000, 3500]\nfor i, length in enumerate(lengths, start=1):\n    if length &gt; 3000:\n        print(f\"#{i}: {length} (long)\")\n</code></pre> Show answer <pre><code>#2: 5000 (long)\n#3: 3500 (long)\n</code></pre> <p>Two things to notice:</p> <ul> <li><code>enumerate(..., start=1)</code> makes the index start at <code>1</code> instead of <code>0</code>.</li> <li>The <code>if</code> filters which items get printed; the loop still visits every item.</li> </ul>"}, {"location": "python-basics/control-flow/#while-loops", "title": "While Loops", "text": "<pre><code>count = 0\nwhile count &lt; 5:\n    print(count)\n    count += 1\n</code></pre> <p>Quick Check: Spot the bug</p> <p>What is wrong with this loop?</p> <pre><code>count = 0\nwhile count &lt; 5:\n    print(count)\n</code></pre> Show answer <p>It's an infinite loop. <code>count</code> is never incremented, so <code>count &lt; 5</code> is always <code>True</code> and <code>0</code> is printed forever. Add <code>count += 1</code> inside the loop body. This is the most common <code>while</code>-loop bug \u2014 always check that the condition can become <code>False</code>.</p>"}, {"location": "python-basics/control-flow/#list-comprehensions", "title": "List Comprehensions", "text": "<p>A concise way to create lists:</p> <pre><code>lengths = [1000, 2500, 5000, 7500]\nlong_elements = [l for l in lengths if l &gt; 3000]\nprint(long_elements)  # [5000, 7500]\n</code></pre> <p>Quick Check: Comprehension to loop</p> <p>Rewrite this list comprehension as a regular <code>for</code> loop:</p> <pre><code>squares = [x * x for x in range(5) if x % 2 == 0]\n</code></pre> <p>What is <code>squares</code>?</p> Show answer <p>Loop form:</p> <pre><code>squares = []\nfor x in range(5):\n    if x % 2 == 0:\n        squares.append(x * x)\n</code></pre> <p><code>range(5)</code> yields <code>0, 1, 2, 3, 4</code>. The even values are <code>0, 2, 4</code>, so <code>squares = [0, 4, 16]</code>.</p> <p>A comprehension and the equivalent loop produce identical results \u2014 comprehensions are just more compact when the body is simple.</p> <p>Warning</p> <p>Be careful with <code>while</code> loops \u2014 ensure the condition eventually becomes <code>False</code> to avoid infinite loops.</p>"}, {"location": "python-basics/control-flow/#conditional-expressions", "title": "Conditional Expressions", "text": "<pre><code>value_if_true if condition else value_if_false\n</code></pre> <pre><code>length = 5000\ncategory = \"Long element\" if length &gt; 6000 else \"Short element\" \nprint(category)  # Short element\n</code></pre> <pre><code>length = 5000\ncategory = \"Long element\" if length &gt; 6000 else \"Medium element\" if length &gt; 3000 else \"Short element\"\nprint(category)  # Medium element\n</code></pre>"}, {"location": "python-basics/control-flow/#wrap-up-exercise", "title": "Wrap-up Exercise", "text": "<p>Mini exercise: Classify a batch</p> <p>Given:</p> <pre><code>lengths = [1500, 3000, 5000, 7200, 800, 6500]\n</code></pre> <p>Write code that produces a dict like <code>{\"short\": 2, \"medium\": 2, \"long\": 2}</code> using the boundaries from the if/elif example above (<code>&gt; 6000</code> = long, <code>&gt; 3000</code> = medium, else short).</p> Show answer <pre><code>counts = {\"short\": 0, \"medium\": 0, \"long\": 0}\nfor length in lengths:\n    if length &gt; 6000:\n        counts[\"long\"] += 1\n    elif length &gt; 3000:\n        counts[\"medium\"] += 1\n    else:\n        counts[\"short\"] += 1\n# {\"short\": 2, \"medium\": 2, \"long\": 2}\n</code></pre> <p>Alternative one-liner using <code>collections.Counter</code>:</p> <pre><code>from collections import Counter\ncounts = Counter(\n    \"long\" if l &gt; 6000 else \"medium\" if l &gt; 3000 else \"short\"\n    for l in lengths\n)\n</code></pre>"}, {"location": "python-basics/data-structures/", "title": "Data Structures", "text": "<p>Python ships with four built-in container types that cover almost every day-to-day task: list, tuple, set, and dict. Picking the right one matters \u2014 each has different strengths for storage, lookup, and mutation.</p> <p></p>"}, {"location": "python-basics/data-structures/#overview", "title": "Overview", "text": "Structure Ordered Mutable Duplicates Lookup Typical Use <code>list</code> Yes Yes Yes O(n) Ordered collection that changes over time <code>tuple</code> Yes No Yes O(n) Fixed record, coordinates, return multiple values <code>set</code> No Yes No O(1) Membership tests, removing duplicates <code>dict</code> Yes* Yes Keys: No O(1) Key-value mapping, named fields <p>* Dictionaries preserve insertion order since Python 3.7.</p> <p>Quick Check: Pick the structure</p> <p>Which built-in type fits best for each scenario?</p> <ol> <li>A 3D coordinate <code>(x, y, z)</code> that should never be changed</li> <li>The set of distinct material codes used in a project</li> <li>The geometric properties of a beam (width, height, length, material)</li> <li>The ordered history of edits the user made</li> </ol> Show answer <ol> <li><code>tuple</code> \u2014 fixed size, immutable, perfect for coordinates.</li> <li><code>set</code> \u2014 automatically deduplicates and offers O(1) lookups.</li> <li><code>dict</code> \u2014 named fields read better than positional values.</li> <li><code>list</code> \u2014 ordered, mutable, supports <code>append</code>.</li> </ol>"}, {"location": "python-basics/data-structures/#lists", "title": "Lists", "text": "<p>Ordered, mutable sequences \u2014 the workhorse for collections of elements.</p> <p>Advantages</p> <ul> <li>Keeps insertion order</li> <li>Fast append and index access</li> <li>Supports slicing and comprehensions</li> </ul> <p></p> <pre><code>elements = [\"beam\", \"panel\", \"column\"]\n\nelements.append(\"plate\")          # add to end\nelements.insert(0, \"foundation\")  # add at position\nelements.remove(\"panel\")          # remove by value\nlast = elements.pop()             # remove and return last\nelements.sort()                   # sort in place\nelements.reverse()                # reverse in place\ncount = elements.count(\"beam\")    # how many times it appears\nidx = elements.index(\"column\")    # first position of value\n</code></pre> <p>Tip</p> <p>Use a list comprehension instead of <code>append</code> in a loop when building a new list: <code>long_beams = [e for e in elements if e.length &gt; 3000]</code></p> <p>Quick Check: List mutation</p> <p>What is the final value of <code>items</code>?</p> <pre><code>items = [\"beam\", \"panel\", \"column\"]\nitems.insert(1, \"plate\")\nitems.remove(\"panel\")\nitems.append(\"wall\")\n</code></pre> Show answer <p><code>[\"beam\", \"plate\", \"column\", \"wall\"]</code></p> <p>Step-by-step:</p> <ol> <li><code>insert(1, \"plate\")</code> \u2192 <code>[\"beam\", \"plate\", \"panel\", \"column\"]</code></li> <li><code>remove(\"panel\")</code> \u2192 <code>[\"beam\", \"plate\", \"column\"]</code></li> <li><code>append(\"wall\")</code> \u2192 <code>[\"beam\", \"plate\", \"column\", \"wall\"]</code></li> </ol>"}, {"location": "python-basics/data-structures/#tuples", "title": "Tuples", "text": "<p>Ordered, immutable sequences. Once created, they cannot be changed.</p> <p>Advantages</p> <ul> <li>Safe to use as dictionary keys or set members</li> <li>Slightly faster and smaller than lists</li> <li>Signals intent: \"this grouping will not change\"</li> </ul> <p></p> <pre><code>point = (1200.0, 500.0, 2500.0)   # x, y, z coordinate\nx, y, z = point                    # tuple unpacking\n\ndimensions = (120, 240, 5000)      # width, height, length\nprint(dimensions[2])               # 5000\n\n# Returning multiple values from a function\ndef bounding_box():\n    return (0, 0, 0), (6000, 240, 120)\n\nstart, end = bounding_box()\n</code></pre> <p>Useful methods:</p> <pre><code>point.count(0)   # how many zeros\npoint.index(500) # position of value\n</code></pre> <p>Quick Check: Tuple immutability</p> <p>Which of these lines will raise an error?</p> <pre><code>point = (1200.0, 500.0, 2500.0)\na = point[0]               # 1\nb = point + (0.0,)         # 2\npoint[0] = 0               # 3\npoint = (0, 0, 0)          # 4\n</code></pre> Show answer <p>Only line 3 raises <code>TypeError: 'tuple' object does not support item assignment</code>.</p> <ul> <li>Line 2 creates a new tuple by concatenation \u2014 the original is untouched.</li> <li>Line 4 just rebinds the name <code>point</code> to a different tuple \u2014 also fine.</li> <li>Immutability means the tuple's contents can't change, not that the variable is frozen.</li> </ul>"}, {"location": "python-basics/data-structures/#sets", "title": "Sets", "text": "<p>Unordered collections of unique elements. Built for fast membership checks and set algebra.</p> <p>Advantages</p> <ul> <li><code>x in my_set</code> is O(1) \u2014 much faster than a list for large data</li> <li>Automatically removes duplicates</li> <li>Native union / intersection / difference operations</li> </ul> <p></p> <pre><code>materials = {\"GL24h\", \"C24\", \"BSH\"}\n\nmaterials.add(\"GL28h\")\nmaterials.discard(\"C24\")     # remove if present, no error if missing\nmaterials.remove(\"BSH\")      # remove, raises KeyError if missing\n\n# Deduplicate a list\nnames = [\"beam\", \"panel\", \"beam\", \"column\", \"panel\"]\nunique = set(names)          # {\"beam\", \"panel\", \"column\"}\n\n# Set algebra\nstructural = {\"GL24h\", \"GL28h\", \"BSH\"}\nin_stock   = {\"GL24h\", \"C24\"}\n\nstructural &amp; in_stock        # intersection -&gt; {\"GL24h\"}\nstructural | in_stock        # union        -&gt; {\"GL24h\", \"GL28h\", \"BSH\", \"C24\"}\nstructural - in_stock        # difference   -&gt; {\"GL28h\", \"BSH\"}\n</code></pre> <p>Quick Check: Set algebra in practice</p> <p>You have two sets:</p> <pre><code>needed   = {\"GL24h\", \"GL28h\", \"C24\"}\nin_stock = {\"GL24h\", \"C24\", \"BSH\"}\n</code></pre> <p>Which set operation answers each question?</p> <ol> <li>Which needed materials are missing from stock?</li> <li>Which materials are in stock but not needed (could be returned)?</li> <li>Which materials we have and need?</li> </ol> Show answer <ol> <li><code>needed - in_stock</code> \u2192 <code>{\"GL28h\"}</code></li> <li><code>in_stock - needed</code> \u2192 <code>{\"BSH\"}</code></li> <li><code>needed &amp; in_stock</code> \u2192 <code>{\"GL24h\", \"C24\"}</code></li> </ol> <p>Set difference is not symmetric: <code>A - B</code> is different from <code>B - A</code>.</p>"}, {"location": "python-basics/data-structures/#dictionaries", "title": "Dictionaries", "text": "<p>Key-value mappings \u2014 think of them as named fields or a lookup table.</p> <p>Advantages</p> <ul> <li>O(1) lookup by key</li> <li>Clear, self-documenting code (<code>beam[\"material\"]</code> beats <code>beam[3]</code>)</li> <li>Flexible: values can be any type, including nested dicts or lists</li> </ul> <p></p> <pre><code>beam = {\n    \"width\": 120,\n    \"height\": 240,\n    \"length\": 5000,\n    \"material\": \"GL24h\",\n}\n\nbeam[\"volume\"] = beam[\"width\"] * beam[\"height\"] * beam[\"length\"]  # add key\nbeam.update({\"material\": \"GL28h\", \"treatment\": \"planed\"})         # bulk update\nmaterial = beam.get(\"material\", \"unknown\")                        # safe lookup\nremoved = beam.pop(\"treatment\")                                   # remove &amp; return\n\n# Iteration\nfor key, value in beam.items():\n    print(f\"{key}: {value}\")\n\nlist(beam.keys())    # all keys\nlist(beam.values())  # all values\n</code></pre> <p>Tip</p> <p>Use <code>dict.get(key, default)</code> when a key may be missing \u2014 it avoids <code>KeyError</code> and makes default values explicit.</p> <p>Quick Check: <code>[]</code> vs. <code>.get()</code></p> <p>What does each of these print, given the dictionary below?</p> <pre><code>beam = {\"width\": 120, \"height\": 240, \"length\": 5000}\n\nprint(beam[\"length\"])              # 1\nprint(beam.get(\"length\"))          # 2\nprint(beam.get(\"treatment\"))       # 3\nprint(beam.get(\"treatment\", \"\u2014\"))  # 4\nprint(beam[\"treatment\"])           # 5\n</code></pre> Show answer <ol> <li><code>5000</code></li> <li><code>5000</code></li> <li><code>None</code></li> <li><code>\u2014</code></li> <li>Raises <code>KeyError: 'treatment'</code> \u2014 the program crashes here.</li> </ol> <p>Use <code>[]</code> when the key must exist; use <code>.get()</code> when it might not.</p>"}, {"location": "python-basics/data-structures/#choosing-the-right-structure", "title": "Choosing the right structure", "text": "<pre><code>flowchart TD\n    A[\"Need to store data\"] --&gt; B{\"Key-value pairs?\"}\n    B --&gt;|Yes| C[\"dict\"]\n    B --&gt;|No| D{\"Unique values only?\"}\n    D --&gt;|Yes| E[\"set\"]\n    D --&gt;|No| F{\"Will it change?\"}\n    F --&gt;|Yes| G[\"list\"]\n    F --&gt;|No| H[\"tuple\"]</code></pre> <p>Note</p> <p>These four structures can be combined freely \u2014 a list of dicts is a common way to represent a table of elements, and a dict of sets works well for grouping. Pick the structure that matches how you will access the data, not just how you will store it.</p>"}, {"location": "python-basics/data-structures/#wrap-up-exercise", "title": "Wrap-up Exercise", "text": "<p>Mini exercise: Element catalog</p> <p>You receive this list of beams:</p> <pre><code>beams = [\n    {\"name\": \"B-01\", \"material\": \"GL24h\", \"length\": 5000},\n    {\"name\": \"B-02\", \"material\": \"GL28h\", \"length\": 3000},\n    {\"name\": \"B-03\", \"material\": \"GL24h\", \"length\": 7000},\n    {\"name\": \"B-04\", \"material\": \"C24\",   \"length\": 2500},\n]\n</code></pre> <p>Write expressions that produce:</p> <ol> <li>The set of distinct materials used.</li> <li>A list of names of beams longer than 4000 mm.</li> <li>The total length of all beams combined.</li> </ol> Show answer <pre><code>materials = {b[\"material\"] for b in beams}\n# {\"GL24h\", \"GL28h\", \"C24\"}\n\nlong_names = [b[\"name\"] for b in beams if b[\"length\"] &gt; 4000]\n# [\"B-01\", \"B-03\"]\n\ntotal = sum(b[\"length\"] for b in beams)\n# 17500\n</code></pre> <p>Notice the set comprehension in (1), list comprehension with filter in (2), and generator expression passed to <code>sum</code> in (3).</p>"}, {"location": "python-basics/data-types/", "title": "Data Types", "text": "<p>Python provides several built-in data types. Understanding them is the foundation for writing any program.</p> <p></p>"}, {"location": "python-basics/data-types/#variables", "title": "Variables", "text": "<p>In Python, variables are created by assigning a value:</p> <pre><code>name = \"cadwork\"\nversion = 30\npi = 3.14159\n</code></pre> <p>Quick Check: Variable assignment</p> <p>What type does Python infer for each of these?</p> <pre><code>a = 30\nb = 30.0\nc = \"30\"\nd = True\n</code></pre> Show answer <ul> <li><code>a</code> \u2192 <code>int</code></li> <li><code>b</code> \u2192 <code>float</code></li> <li><code>c</code> \u2192 <code>str</code></li> <li><code>d</code> \u2192 <code>bool</code></li> </ul> <p>Python infers the type from the value on the right-hand side \u2014 no declaration needed.</p>"}, {"location": "python-basics/data-types/#numbers", "title": "Numbers", "text": "<p>Python supports integers and floating-point numbers:</p> <pre><code>count = 42          # int\nheight = 3.5        # float\nresult = count + 10 # arithmetic\n</code></pre> <p>Quick Check: Integer vs. float division</p> <p>What are the values of <code>a</code> and <code>b</code>?</p> <pre><code>a = 7 / 2\nb = 7 // 2\n</code></pre> Show answer <ul> <li><code>a</code> \u2192 <code>3.5</code> (true division, always returns a <code>float</code>)</li> <li><code>b</code> \u2192 <code>3</code> (floor division, drops the remainder)</li> </ul> <p>Tip: <code>7 % 2</code> gives the remainder (<code>1</code>).</p>"}, {"location": "python-basics/data-types/#strings", "title": "Strings", "text": "<p>Strings are sequences of characters:</p> <pre><code>greeting = \"Hello, World!\"\nprint(greeting.upper())  # HELLO, WORLD!\nprint(len(greeting))     # 13\n</code></pre> <p>Quick Check: String operators</p> <p>What does this print?</p> <pre><code>prefix = \"GL\"\ngrade = 24\nname = prefix + str(grade) + \"h\"\nprint(name * 2)\n</code></pre> Show answer <pre><code>GL24hGL24h\n</code></pre> <p><code>+</code> concatenates strings, <code>*</code> repeats them. Note: <code>str(grade)</code> is required \u2014 Python will raise <code>TypeError</code> if you try to concatenate <code>str + int</code> directly.</p>"}, {"location": "python-basics/data-types/#references-and-mutability", "title": "References and Mutability", "text": "<p>When you write <code>name = value</code>, Python does not copy the value into a box called <code>name</code>. Instead, <code>name</code> becomes a label that points to the value. Think of variable names like sticky notes attached to things on a shelf \u2014 several notes can stick to the same thing.</p> <pre><code>beam_a = [120, 240, 5000]\nbeam_b = beam_a               # same list, two names\n</code></pre> <p>Both <code>beam_a</code> and <code>beam_b</code> now point to the same list. Modifying the list through one name is visible through the other:</p> <pre><code>beam_b.append(\"GL24h\")\nprint(beam_a)                 # [120, 240, 5000, 'GL24h']\n</code></pre> <p>This surprises everyone the first time they see it.</p>"}, {"location": "python-basics/data-types/#mutable-vs-immutable-types", "title": "Mutable vs. immutable types", "text": "Type Mutable? Example <code>int</code>, <code>float</code>, <code>bool</code> No <code>x = x + 1</code> creates a new number <code>str</code> No <code>s.upper()</code> returns a new string <code>tuple</code> No Cannot change items after creation <code>list</code> Yes <code>lst.append(...)</code>, <code>lst[0] = ...</code> <code>dict</code> Yes <code>d[\"key\"] = value</code>, <code>d.pop(...)</code> <code>set</code> Yes <code>s.add(...)</code>, <code>s.remove(...)</code> <ul> <li>Mutable types can be changed in place \u2014 every name pointing to them sees the change.</li> <li>Immutable types cannot be changed in place \u2014 operations always produce a new object.</li> </ul>"}, {"location": "python-basics/data-types/#immutable-behaves-intuitively", "title": "Immutable behaves intuitively", "text": "<pre><code>length_a = 5000\nlength_b = length_a           # both labels point to 5000\n\nlength_b = length_b + 1000    # length_b now points to a NEW int (6000)\nprint(length_a)               # 5000 \u2014 unaffected\n</code></pre> <p>Because <code>int</code> is immutable, <code>length_b + 1000</code> builds a fresh integer and re-binds the label <code>length_b</code> to it. The original <code>5000</code> is untouched, so <code>length_a</code> still sees it.</p>"}, {"location": "python-basics/data-types/#when-you-want-a-real-copy", "title": "When you want a real copy", "text": "<pre><code>beam_a = [120, 240, 5000]\nbeam_b = beam_a.copy()        # NEW list, independent\n\nbeam_b.append(\"GL24h\")\nprint(beam_a)                 # [120, 240, 5000]            \u2014 untouched\nprint(beam_b)                 # [120, 240, 5000, 'GL24h']\n</code></pre> <p>Other ways to copy a list: <code>list(beam_a)</code>, or the slice <code>beam_a[:]</code>. For dicts use <code>d.copy()</code> or <code>dict(d)</code>. For sets use <code>s.copy()</code> or <code>set(s)</code>.</p> <p>Quick Check: Predict the output</p> <p>What does the second <code>print</code> show?</p> <pre><code>a = [1, 2, 3]\nb = a\nb[0] = 99\nprint(a)\n</code></pre> Show answer <p><code>[99, 2, 3]</code></p> <p><code>b = a</code> does not copy the list \u2014 both names point to the same list. Changing element <code>0</code> through <code>b</code> is visible through <code>a</code>. To get an independent list, write <code>b = a.copy()</code>.</p> <p>Quick Check: Why is this different?</p> <p>What does this print?</p> <pre><code>a = 10\nb = a\nb = 99\nprint(a)\n</code></pre> Show answer <p><code>10</code></p> <p><code>int</code> is immutable. <code>b = 99</code> does not modify the original number \u2014 it just rebinds the label <code>b</code> to a different integer. <code>a</code> still labels the original <code>10</code>.</p> <p>Rule of thumb</p> <p>When you pass a <code>list</code>, <code>dict</code>, or <code>set</code> into a function, the function can change it. When you pass an <code>int</code>, <code>float</code>, <code>str</code>, or <code>tuple</code>, it cannot. If you want to be sure a function does not change your data, pass a copy.</p>"}, {"location": "python-basics/functions/", "title": "Functions", "text": "<p>Functions let you organize code into reusable blocks.</p> <p></p>"}, {"location": "python-basics/functions/#defining-a-function", "title": "Defining a Function", "text": "<pre><code>def calculate_volume(width, height, length):\n    \"\"\"Calculate the volume of a rectangular element.\"\"\"\n    return width * height * length\n\nvolume = calculate_volume(120, 240, 5000)\nprint(f\"Volume: {volume} mm\u00b3\")\n</code></pre> <p>Quick Check: Positional vs. keyword arguments</p> <p>Given <code>calculate_volume(width, height, length)</code>, which of these calls produce the same result?</p> <pre><code>a = calculate_volume(120, 240, 5000)\nb = calculate_volume(240, 120, 5000)\nc = calculate_volume(length=5000, width=120, height=240)\nd = calculate_volume(120, length=5000, height=240)\n</code></pre> Show answer <p><code>a</code>, <code>c</code>, and <code>d</code> all return <code>144_000_000</code>. <code>b</code> returns the same number too \u2014 because multiplication is commutative \u2014 but the meaning is wrong (width and height are swapped).</p> <p>Key idea: positional arguments rely on order, keyword arguments rely on names. Mixing both is fine, but every positional must come before any keyword.</p>"}, {"location": "python-basics/functions/#default-parameters", "title": "Default Parameters", "text": "<pre><code>def greet(name, greeting=\"Hello\"):\n    return f\"{greeting}, {name}!\"\n\nprint(greet(\"cadwork\"))          # Hello, cadwork!\nprint(greet(\"cadwork\", \"Hi\"))    # Hi, cadwork!\n</code></pre> <p>Quick Check: The mutable default trap</p> <p>What does this print? Most people get it wrong on the first try.</p> <pre><code>def add_tag(tag, tags=[]):\n    tags.append(tag)\n    return tags\n\nprint(add_tag(\"structural\"))\nprint(add_tag(\"visible\"))\n</code></pre> Show answer <pre><code>[\"structural\"]\n[\"structural\", \"visible\"]\n</code></pre> <p>Why: The default value <code>[]</code> is created once, when the function is defined \u2014 and reused across every call that doesn't pass <code>tags</code>. The same list keeps growing.</p> <p>Fix: use <code>None</code> as a sentinel:</p> <pre><code>def add_tag(tag, tags=None):\n    if tags is None:\n        tags = []\n    tags.append(tag)\n    return tags\n</code></pre> <p>Rule of thumb: never use a mutable object (<code>list</code>, <code>dict</code>, <code>set</code>) as a default value.</p>"}, {"location": "python-basics/functions/#returning-multiple-values", "title": "Returning Multiple Values", "text": "<pre><code>def min_max(values):\n    return min(values), max(values)\n\nlowest, highest = min_max([100, 250, 50, 400])\nprint(f\"Min: {lowest}, Max: {highest}\")\n</code></pre> <p>Quick Check: What is actually returned?</p> <p>The function appears to return two values. What's the real return type?</p> Show answer <p>It returns one <code>tuple</code> with two elements. The line <code>return min(values), max(values)</code> is syntactic sugar for <code>return (min(values), max(values))</code>.</p> <p>The receiving side uses tuple unpacking to split it into <code>lowest</code> and <code>highest</code>. You could also write:</p> <pre><code>result = min_max([100, 250, 50, 400])\nprint(result)         # (50, 400)\nprint(type(result))   # &lt;class 'tuple'&gt;\n</code></pre>"}, {"location": "python-basics/functions/#modules-and-imports", "title": "Modules and Imports", "text": "<p>Python code can be organized into modules:</p> <pre><code>flowchart LR\n    subgraph geometry.py\n        A[\"area(w, h)\"]\n        B[\"perimeter(w, h)\"]\n    end\n    subgraph main.py\n        C[\"from geometry import area\"]\n        D[\"result = area(120, 240)\"]\n    end\n    A -.-&gt;|import| C</code></pre> <pre><code># geometry.py\ndef area(width, height):\n    return width * height\n\n# main.py\nfrom geometry import area\n\nresult = area(120, 240)\n</code></pre> <p>Tip</p> <p>Keep functions small and focused on a single task. This makes your code easier to read, test, and reuse.</p>"}, {"location": "python-basics/functions/#wrap-up-exercise", "title": "Wrap-up Exercise", "text": "<p>Mini exercise: Write a function</p> <p>Write a function <code>summarize(elements)</code> that takes a list of beam dicts (like below) and returns a tuple <code>(count, total_length, materials)</code> where <code>materials</code> is the set of distinct material codes.</p> <pre><code>elements = [\n    {\"name\": \"B-01\", \"material\": \"GL24h\", \"length\": 5000},\n    {\"name\": \"B-02\", \"material\": \"GL28h\", \"length\": 3000},\n    {\"name\": \"B-03\", \"material\": \"GL24h\", \"length\": 7000},\n]\n# summarize(elements) -&gt; (3, 15000, {\"GL24h\", \"GL28h\"})\n</code></pre> Show answer <pre><code>def summarize(elements):\n    count = len(elements)\n    total_length = sum(e[\"length\"] for e in elements)\n    materials = {e[\"material\"] for e in elements}\n    return count, total_length, materials\n\ncount, total, mats = summarize(elements)\n</code></pre> <p>One-liner alternative:</p> <pre><code>def summarize(elements):\n    return (\n        len(elements),\n        sum(e[\"length\"] for e in elements),\n        {e[\"material\"] for e in elements},\n    )\n</code></pre>"}, {"location": "resources/", "title": "Resources", "text": "<p>A collection of useful links and references for Python programming and the cadwork 3d API.</p>"}, {"location": "resources/#python", "title": "Python", "text": "<ul> <li>Python Official Documentation</li> <li>Python Tutorial</li> <li>Real Python</li> </ul>"}, {"location": "resources/#cadwork", "title": "cadwork", "text": "<ul> <li>cadwork Website</li> <li>cadwork Python API Documentation</li> </ul>"}, {"location": "resources/#mcp", "title": "MCP", "text": "<ul> <li>Model Context Protocol Specification</li> </ul>"}, {"location": "resources/#tools", "title": "Tools", "text": "<ul> <li>Visual Studio Code \u2014 Recommended code editor</li> <li>uv \u2014 Python package and project manager</li> </ul>"}]}